# DeltaNet prefill research (Sep 2 night, 38 agents; spec stage cut off by usage limit)

Verified findings from the mining + verification stages. The two spec-writers did not run; these are the inputs for the chunkwise / re-tiled prefill design.


### 1. Exact gated delta recurrence in YOUR layout (S is K x V, thread j owns column S[:,j]) and its closed-form unrolling over a chunk — this is the identity everything else follows from.

Per value head, tokens r=1..C in the chunk, S_0 = state after the previous token. Inputs: q_r,k_r in R^128 (L2-normalised, q additionally scaled by 128^-0.5), v_r in R^128, beta_r=sigmoid(b_r) in (0,1), g_r = -exp(A_log)*softplus(a_r+dt_bias) <= 0, alpha_r = exp(g_r).
Sequential (what dn_delta_mc does): S_r = alpha_r*S_{r-1} + k_r d_r^T,  d_r = beta_r*(v_r - alpha_r*S_{r-1}^T k_r),  o_r = S_r^T q_r.
Define G_r = sum_{m<=r} g_m, gamma_r = exp(G_r) (gamma_0=1), and the pairwise decay Lambda[r,i] = gamma_r/gamma_i = prod_{m=i+1..r} alpha_m for i<=r (Lambda[r,r]=1), 0 for i>r.
Unrolled: S_r = gamma_r*S_0 + sum_{i<=r} Lambda[r,i] k_i d_i^T.
Substituting into d_r gives a unit-lower-triangular linear system for the C pseudo-values d_r (rows of D, C x 128):
  d_r = beta_r v_r - beta_r gamma_r S_0^T k_r - sum_{i<r} beta_r Lambda[r,i] (k_r . k_i) d_i.
This is the WY/UT-transform: the C Householder-like factors prod alpha_i(I - beta_i k_i k_i^T) collapse into I - sum w_i k_i^T with pseudo-values solved by one small triangular solve (DeltaNet paper Sec. 3, Eq. 6; Gated DeltaNet Sec. 3.3: u~_r = beta_r(v_r - sum_{i<r} u~_i (gamma_r/gamma_i) k_i^T k_r)).

_Source:_ arXiv 2406.06484 Sec 3 Eq.(6)-(10); arXiv 2412.06464 Sec 3.3; verified against fla/ops/gated_delta_rule/naive.py naive_recurrent_gated_delta_rule (h = h*exp(g); v -= h^T k; v *= beta; h += k v^T; o = q^T h) which is exactly your kernel.

_Actionable:_ Keep your S layout (K x V, column-per-thread); the chunk form below is written directly in that layout so no transposes are needed. Everything in the chunk is separable per V-column j, so the column-per-thread mapping survives.


### 2. Chunkwise algorithm, exact matrix form (C tokens, K=V=128), in your layout. Four equations; only three of them touch S.

Let Q,K,V in R^{C x 128} (rows = tokens), beta, gamma in R^C, K_beta = diag(beta) K, V_beta = diag(beta) V.
(1) Lambda (C x C): Lambda[r,i] = exp(G_r - G_i) for i<=r else 0. Lambda_strict = same with the diagonal zeroed.
(2) A = (K_beta K^T) ⊙ Lambda_strict   (C x C, strictly lower; A[r,i] = beta_r * Lambda[r,i] * (k_r . k_i), i<r).
    T = (I + A)^{-1}   (unit lower triangular; paper Eq.10 is T = (I + tril(diag(beta) K K^T, -1))^{-1} diag(beta) — the diag(beta) is folded into K_beta/V_beta in code). NOTE the sign: it is I PLUS the strictly-lower part of diag(beta)KK^T ⊙ Lambda; the '(I - tril(...))^{-1}' in your prompt is a sign slip (FLA writes attn = -(k_beta@k^T)⊙L then substitutes, which is the same thing).
(3) Pseudo-values / delta targets:  D = T * (V_beta - diag(gamma) K_beta S_0)          (C x 128)
    Equivalently the FLA/HF factored form: U = T V_beta ('new_values'), W = T diag(gamma) K_beta ('k_cumdecay'), D = U - W S_0. NB: decay is applied to K_beta BEFORE T (T diag(gamma) K_beta), not after — this follows from the derivation and matches the code.
(4) Output:  O = diag(gamma) Q S_0 + ((Q K^T) ⊙ Lambda) D           (C x 128; the mask INCLUDES the diagonal because o_r reads S_r after its own update).
(5) New state:  S_C = gamma_C S_0 + (diag(gamma_C/gamma) K)^T D  = gamma_C S_0 + sum_r Lambda[C,r] k_r d_r^T   (128 x 128).
Ungated special case (gamma=1) is exactly the DeltaNet paper's Eq.(8)-(9): S_[t+1] = S_[t] + (U - W S_[t]^T)^T K, O = Q S_[t]^T + (Q K^T ⊙ M)(U - W S_[t]^T) (paper's S is V x K; yours is the transpose, hence W S_0 instead of W S^T).
The state S_C produced by (5) is algebraically identical to running the sequential recurrence over the C tokens, so decode continues from it unchanged.

_Source:_ fla/ops/gated_delta_rule/naive.py naive_chunk_gated_delta_rule (verbatim: L_mask=((decay_i - decay_j).tril().exp()).tril(); attn=-((k_beta@k^T)*L_mask) with diagonal masked; forward-substitution loop; k_cumsum=attn@v; k_cumdecay=attn@(k_beta*decay_exp); v_new=v_i - k_cumdecay@S; o = (q_i*exp(decay))@S + attn_qk@v_new; S = S*exp(decay_last) + (k_i*exp(decay_last-decay))^T @ v_new). Same in transformers qwen3_next torch_chunk_gated_delta_rule (pairwise_decay masked to -inf then exp; solve_triangular(unitriangular=True); key *= exp(cum_decay[-1]-cum_decay); chunk_decay=exp(cum_decay[-1])).

_Actionable:_ Implement form (3) (fold S_0 into the RHS and do ONE triangular solve) when a pass = one chunk: it needs one read of S (for both diag(gamma)K_beta S_0 and diag(gamma)Q S_0, fused), one C-step forward substitution per column, one C x C by C x 128 product, one rank-C update + write of S. Use the factored U/W form only if you want to precompute the S-independent part for many chunks in one high-occupancy dispatch (that is what FLA does with chunk_size 64).


### 3. The triangular solve is trivial at C<=16: forward substitution row by row, no pivoting, always well-defined (unit diagonal).

Row-recursive form used by both FLA naive code and the HF export path (verbatim: attn = -A; for i in 1..C-1: attn[i,:i] += sum_j attn[i,j]*attn[j,:i]; attn += I). That computes X = T - I via x_r = -a_r - sum_{i<r} a_{r,i} x_i. You do not even need T: solve (I+A) D = R directly per column j: for r=0..C-1: D[r,j] = R[r,j] - sum_{i<r} A[r,i]*D[i,j]. Cost per column: C(C-1)/2 FMAs (120 at C=16, 28 at C=8), serial in r but each step is a length-<=15 dot, versus the sequential kernel's 128-length dependent reductions per token. FLA's production kernel (solve_tril.py) inverts (I+A) in fp32 ('IEEE' dot precision) via 16x16 forward substitution plus block-Schur merges for 32/64 — that machinery only matters for C>=32. Stability: with ||k||_2 = 1 and beta in (0,1), the eigenvalues of I - beta k k^T are 1 (mult. 127) and 1-beta (DeltaNet paper Sec 3.1), the product is a contraction, and entries of A satisfy |A[r,i]| <= beta_r*Lambda[r,i] < 1; the solve is benign in f32 for C<=16.

_Source:_ fla/ops/delta_rule/naive.py delta_rule_chunkwise; fla/ops/utils/solve_tril.py (solve_tril_16x16_kernel, merge_16x16_to_32x32/64x64, output fp32); arXiv 2406.06484 Sec 3.1 eigenvalue remark, 'inverse of lower triangular matrices could be solved efficiently using forward substitution'.

_Actionable:_ Do the solve in f32 in-thread (no barriers needed once A is in workgroup memory). Never compute T explicitly for C<=16; substitute directly against the RHS columns.


### 4. Shape / work map for dState=128, chunk C in {4,8,16}, per value head h, per pass — and a concrete WGSL workgroup plan that keeps your column-per-thread mapping.

Shared (workgroup) memory: Ks[C][128] f32 (K rows) = 2/4/8 KB for C=4/8/16; Qs[C][128] same (or read Q rows straight from the storage buffer: uniform address across threads = broadcast, cached); A[C][C], P[C][C], Lambda[C][C], beta[C], gamma[C], lamC[C] (= Lambda[C-1][r]) — <=2 KB. Total 6/10/18 KB (C=4/8/16 with Q in shared) — fits 32 KB; at 16 KB budget keep Q in storage or use C<=8.
Registers per thread (thread j = V column): s[128] (its S column), R[C], D[C], Oi[C].
Steps (per pass, one workgroup of 128 threads per head; barriers marked):
 S0. Load K,Q rows into shared (thread j loads element j of every row: coalesced). Compute G_r, gamma_r, Lambda (thread 0 or every thread redundantly, C^2 exps). BARRIER.
 S1. Pairwise dots: assign pairs to threads. A[r][i] = beta_r*Lambda[r][i]*dot(Ks[r],Ks[i]) for i<r: C(C-1)/2 dots (6/28/120) of length 128; P[r][i] = Lambda[r][i]*dot(Qs[r],Ks[i]) for i<=r: C(C+1)/2 (10/36/136). At C=16 that is 256 dot products -> exactly 2 per thread. BARRIER.
 S2. s[i] = S[h][i][j], i=0..127 (row-major S -> coalesced across j). 1 read of the 64 KB state per head per pass.
 S3. For r in 0..C-1: ak = sum_i Ks[r][i]*s[i]; aq = sum_i Qs[r][i]*s[i]; R[r] = beta_r*V[r][j] - beta_r*gamma_r*ak; Oi[r] = gamma_r*aq.  2*C*128 FMAs with 2C independent accumulators (the sequential kernel has a 128-long dependent chain per token instead). Shared reads are broadcast (all threads read the same address) -> no bank conflicts.
 S4. Forward substitution: D[r] = R[r] - sum_{i<r} A[r][i]*D[i].  C(C-1)/2 FMAs, no barrier.
 S5. o[r][j] = Oi[r] + sum_{i<=r} P[r][i]*D[i]; write out (feeds the gated RMSNorm unchanged). C(C+1)/2 FMAs.
 S6. s[i] = gamma_{C-1}*s[i] + sum_r lamC[r]*Ks[r][i]*D[r]  (precompute e[r] = lamC[r]*D[r] once); write S[h][i][j].  C*128 FMAs; 1 write of S.
Totals per thread per pass: ~3*C*128 + C^2 FMAs — the SAME FMA count as the sequential kernel (vhat, update, o = 3*128 per token); the gain is (a) all 128-length loops become C-way independent (ILP instead of C*3 dependent 128-chains), (b) S read/written once per pass instead of touched per token, (c) the only serial part is the C-step substitution over C-length rows.
Matrix-op view (per head): [2C x 128]·[128 x 128] (S3), C x C unit-lower solve on a C x 128 RHS (S4), [C x C]·[C x 128] (S5), rank-C update of 128 x 128 (S6); plus S-independent [C x 128]·[128 x C] twice (S1).
Occupancy option: split the 128 K-rows across P threads per column (256 or 512 threads per head, 64 or 32 s-registers each). S3 then needs a shared partial-sum buffer [P][2C][128] f32 (P=2,C=16: 32 KB; P=2,C=8: 16 KB; P=4,C=8: 32 KB) and one barrier; S4/S5 are done redundantly by the P threads of a column (they all read the reduced R), S6 each thread updates its own 128/P slice. This also fixes the 128-f32 private array problem: WGSL var<private> arrays with dynamic indexing spill to scratch on Tint/Metal — fully unroll with constant indices, and prefer 32-64 elements per thread.
GQA note: q,k are per key-head but beta and g are per value-head, so A and P (the Lambda and beta scaling) are per value head; only the raw dot products k_r.k_i and q_r.k_i are shareable across the nVH/nKH heads of a group.

_Source:_ Derived from equations above; FLA chunk kernels use the same decomposition (recompute_w_u = T applied to k*beta*exp2(g) and v*beta; chunk_delta_h state update; chunk_fwd_o).

_Actionable:_ Replace the token loop in dn_delta_mc with S0-S6 for C = tokens-per-pass (4/8, or 16 if you widen the GEMV batch). If a pass ever carries more tokens than C, loop S0-S6 over sub-chunks inside the same dispatch with s[] staying in registers. dn_conv_mc (3-tap causal conv with carried state), the L2 norms, beta/g computation and the gated RMSNorm are per-token and unchanged.


### 5. Numerical caveat 1 (most important for your relDiff~1e-7 validation): compute the pairwise decays as products of per-token alpha, not as exp of differences of a cumulative log-sum.

FLA/HF compute cum_decay = cumsum(g) in f32 (FLA additionally scales by 1/ln2 and uses exp2) and then exp(cum_i - cum_j). For Qwen3-Next g = -exp(A_log)*softplus(a+dt_bias) with A_log initialised log U(0.01,16), so |g_t| can be O(1-100) and G_r over 16 tokens can reach hundreds; f32 absolute error of G is ~|G|*6e-8, which becomes a ~1e-6..1e-5 RELATIVE error in exp(G_r - G_i) — larger than the rounding of everything else. Your sequential kernel multiplies S by alpha_t = exp(g_t) each token, so the bit-closest chunk implementation forms Lambda[r,i] = alpha_{i+1}*...*alpha_r as a running product (C(C-1)/2 multiplies, negligible) and gamma_r = Lambda[r,0]. Also: only exponents <= 0 are ever needed (i<=r); never evaluate exp for i>r — the HF code masks those to -inf before exp precisely because exp of a positive difference can overflow. Underflow: gamma_r can hit f32 denormal/zero for G < -87/-103; the sequential kernel has the identical behaviour (S*alpha repeatedly), and WGSL backends may flush denormals — same on both paths, so no divergence beyond what you already accept.

_Source:_ transformers qwen3_next torch_chunk_gated_delta_rule ('Positive values are masked to -inf before exp to avoid overflow'; cum_decay = decay.cumsum); fla chunk.py chunk_local_cumsum(..., scale=RCP_LN2) with exp2 in wy_fast.py; Qwen3NextGatedDeltaNet: beta = b.sigmoid(); g = -A_log.float().exp() * softplus(a.float() + dt_bias).

_Actionable:_ In S0 compute alpha_r = exp(g_r) once, then Lambda by running products; use Lambda[C-1][r] for the K decay in S6 and Lambda[C-1][0] for gamma_C. Compute beta, g, alpha in f32 exactly as the sequential kernel does (same expression, same order) so the per-token scalars are bit-identical between paths.


### 6. Numerical caveat 2: expected deviation vs the sequential recurrence and how to validate it.

The chunk form reorders sums: o_r accumulates sum_{i<=r} Lambda (k_i.q_r) d_i at once instead of progressively through S, and S_C accumulates rank-C at once. In f32 with L2-normalised q/k (|k_r.k_i|<=1, |beta|<1) expect max relDiff ~1e-6 on outputs and on the final state (vs ~1e-7 you see for your batched-vs-sequential today), i.e. ~10x looser but still ~1000x below the bf16/int quantisation noise of the weights. Cancellation in d_r = beta(v - predicted) is identical in both forms. FLA keeps A, the solve, the pseudo-values and the state in f32 even when q/k/v are bf16 — do not drop any intermediate (A, P, D, s[]) to f16. Padding is exact: a padded token with q=k=v=0, beta=0, g=0 gives d=0, alpha=1 -> S unchanged, o=0, so zero-pad short passes to C and discard pad outputs (this is what FLA/HF do).

_Source:_ fla naive_chunk_gated_delta_rule (all inputs cast to float32, F.pad with zeros); HF torch_chunk_gated_delta_rule (fp32 throughout, pad_size zero padding); fla solve_tril output_dtype=torch.float.

_Actionable:_ Validate per layer: max relDiff of o and of S_C after a C-token pass vs running dn_delta_mc token by token from the same S_0; then greedy-token agreement over a few hundred tokens of a real prompt. Expect 1e-6-ish; anything >1e-4 means the decay ratio or the diagonal-mask convention (strict for A, inclusive for P) is wrong.


### 7. Exact surrounding definitions you must match (Qwen3-Next reference), since q/k normalisation and beta/g feed both paths.

l2norm(x) = x * rsqrt(sum(x^2) + 1e-6) (eps INSIDE the rsqrt on the sum of squares, not on the norm), applied to q and k in f32 (use_qk_l2norm_in_kernel=True); then q *= k_head_dim^-0.5 (=128^-0.5) — linear in q, so it can be applied anywhere. beta = sigmoid(b), g = -exp(A_log)*softplus(a + dt_bias) per value head, f32. Gated RMSNorm: y = (x * rsqrt(mean(x^2)+eps)) * weight * silu(z) — gate multiplied AFTER the weight, in f32. Reference chunk_size: HF default 64; FLA kernel asserts chunk_size in {16,32,64}; DeltaNet paper 'C is usually 64 or 128'. Nothing in the algorithm requires C to be a power of two or >=16 — the papers' choices are tensor-core tile sizes; for you C should equal tokens-per-pass.

_Source:_ transformers/models/qwen3_next/modeling_qwen3_next.py (l2norm, Qwen3NextRMSNormGated, Qwen3NextGatedDeltaNet.forward, chunk_size=64); fla chunk.py ('chunk_size must be 16, 32, or 64 for Gated Delta Rule'); arXiv 2406.06484 Sec 3.

_Actionable:_ Reuse your existing per-token norm/beta/g code verbatim in front of the chunk kernel; only the S-dependent core changes.


### 8. Caution: the chunk form does not reduce FMAs, so confirm dn_delta_mc really is the 37 ms/token before rewriting.

Your numbers: 4-col pass ~148 ms, 8-col ~296 ms -> ~37 ms marginal per prompt token, vs 111 ms per decode token. A column-per-thread sequential kernel doing 3*128 FMAs per token per thread across 48 layers x nVH heads should cost on the order of a few ms per token even when latency-bound, unless (a) s[] lives in global/scratch memory and each token performs a dependent chain of 128 global loads, or (b) something else (conv, norms, attention layers, per-token dispatches) scales linearly with tokens. The chunk kernel removes the dependent 128-chains (ILP over C accumulators) and touches S once per pass, which is exactly the fix for (a); it does nothing for (b).

_Source:_ Analysis of the numbers in the task context; FMA counts from the shape map above.

_Actionable:_ Use WebGPU timestamp queries (or run the pass with dn_delta_mc stubbed to a no-op) to attribute the 37 ms/token. If dn_delta_mc dominates, the chunk kernel should bring the pass close to the decode cost (weight-stream bound), i.e. prefill approaching C x decode throughput; if it does not dominate, look at per-token dispatch counts and the conv/norm kernels first.


### 9. Premise check (measured on this GB10): the DeltaNet recurrence is NOT what bounds prefill; the batched matvec (_b) kernels are. dn_delta_mc is ~1-4% of a pass.

Ran the engine's own bench_breakdown_deno.js (skip-family timing) on the real Qwen3.5-27B GGUF. 4-column pass: 132.7 ms total (single-token pass 112.2 ms); skipping all matvec_*_b saves 91.2 ms; skipping dn_delta_mc saves ~1.3 ms (noise floor is +-5 ms; several families show negative cost). 8-column pass (batchCols: 8): 216.1 ms total; matvec_b family 163.3 ms; dn_delta_mc 7.7 ms; attention 4.8 ms; everything else within noise. So 4->8 columns nearly doubles the GEMV cost (91->163 ms) even though guKernelB dequantizes each weight once and applies it to all COLS columns: at 8 cols it carries ROWSB*COLS*2 = 64 f32 accumulators + 16 vec4 x loads per iteration in 64-thread workgroups, i.e. it has become ALU/register bound, not bandwidth bound (memory floor for 16 GB Q8 weights at 273 GB/s = 59 ms; single-token matvec share is ~80 ms). The non-matvec residual (~41-53 ms, largely ~1000+ small dispatches per pass) is the second-largest fixed cost. The user's '8-col == 4-col tok/s' observation is reproduced (30 vs 37 tok/s here) but the cause is the GEMV column scaling, not the recurrence.

_Source:_ scratch/breakdown.txt and breakdown8.txt (from engine/bench_breakdown_deno.js, 8-col variant in scratchpad/bench_breakdown8.js); kernel at engine/engine.js:541-600 (guKernelB)

_Actionable:_ Re-target the 2-4x prefill effort at matvec_*_b for 8-16 columns (e.g. fewer rows per WG with more columns, 2-stage x staging in workgroup memory, or a small tile-GEMM path for prefill), and at the ~50 ms non-matvec dispatch floor. Any dn_delta_mc rewrite caps out at ~1.04x end-to-end prefill.


### 10. Model/kernel dimensions used for all estimates: nVH=48 value heads, nKH=16 key heads, dState=128, 48 DeltaNet layers; S = 64 KB/head, 3 MB/layer, 144 MB total f32.

Read from q38/model.gguf metadata: qwen35.ssm.time_step_rank=48 (nVH), group_count=16 (nKH), state_size=128, inner_size=6144, conv_kernel=4, block_count=65 (64 + 1 nextn/MTP layer), full_attention_interval=4 (16 attention layers), embedding_length=5120. convDim = 2*2048+6144 = 10240. Current dn_delta_mc: 48 workgroups x 128 threads (thread j owns column j of S) = 6144 threads total = 192 warps across GB10's 48 SMs -> ~4 warps/SM (~6% occupancy), and the per-token inner loops do load S -> mul -> store S then load S -> fma -> store S through the read_write storage buffer (compiler cannot prove no aliasing between iterations, so loads serialize behind stores): 4 x 64 KB of global S traffic per head-token = 12 MB/layer-token, 48 MB per layer per 4-token pass, ~2.3 GB per pass, but L2-resident (3 MB per layer), so the kernel is latency-bound, not bandwidth-bound.

_Source:_ GGUF metadata via scratch/meta.mjs; kernel at engine/qwen35.js:294-333; dispatch at qwen35.js:1097

_Actionable:_ Use nVH=48 (not 16-40) in any occupancy math; parallelism without row-splitting is capped at 48*128 = 6144 column-threads regardless of how workgroups are carved.


### 11. Standalone microbenchmark of the requested variants (48 layers x 4 tokens, exact same math, S validated vs current kernel): register-resident 2D tiling with 512 threads/head is the best, 1.85x over the current kernel; 1024 threads is slower than 512.

Synthetic-data Deno WebGPU bench with 48 distinct 3 MB layer states, 10 passes averaged, relDiff of final S and o vs the current kernel: V0 current (128 thr, S global): 9.18 ms/pass = 191 us/layer = 48 us per token-layer. V1 same decomposition but the S column kept in a private array across the token loop, dynamic loop: 9.68 ms (no gain: dynamic-indexed private array goes to local memory). V1u same with the 128-iteration loops unrolled at WGSL-generation time (indices become constants -> registers): 8.08 ms (1.14x). V2 256 threads = 2 row-groups x 128 columns, each thread owns 64 rows of one column in registers, partial vhat/sq exchanged through 2x128x2 f32 of workgroup memory: 7.46 ms (1.23x). V3 512 threads = 4x128 tiles, 32 S values/thread: 4.97 ms (1.85x). V4 1024 threads = 8x128 tiles: 5.94 ms (1.55x, worse than V3: 8-way partial reduction + 3 barriers per token dominate once the per-thread chain is only 16 rows). Numerics: relDiff S = 4.6e-8..4.9e-8, o = 2.0e-7..2.3e-7 for the tiled variants (partial-sum reordering only); V1/V1u are bit-identical to V0. Memory traffic of register variants: S read once and written once per pass = 2 x 3 MB/layer = 288 MB/pass (~1 ms at 273 GB/s) vs 2.3 GB (L2) for V0. Note the microbench V0 (9.2 ms) is higher than the in-engine skip delta (1.3-7.7 ms), i.e. in the engine the kernel is even less significant.

_Source:_ scratch/dnbench.js (run with `deno run --unstable-webgpu --allow-env dnbench.js`, env NC=4|8|16, NL=48); results in the first NC=4 run output (V0-V4 rows). The NC=8/16 runs were still executing when this report was forced to finalize (scratchpad/micro.txt).

_Actionable:_ If dn_delta_mc is rewritten anyway, implement V3: @workgroup_size(512), lid -> (r = lid/128, j = lid%128), thread owns S[r*32..r*32+32, j] in a private array with generation-time unrolled loops, stage k/q for the token in workgroup memory (1 KB), per token: decay+partials -> 4x128x2 f32 partials in workgroup memory -> barrier -> each thread sums 4 partials for its column, d = (v_j - vhat_j)*beta, S[i,j] += k_i*d, row-group 0 writes o_j = (sq_j + d*kq)*scale; write S back once after the token loop; snapshot (dlm_shadow) is a plain store of the private array per token. Same bind groups and buffers, ~150 lines, drop-in for qwen35.js:294-333 with dispatch count unchanged (48 WGs). Requires requestDevice with maxComputeInvocationsPerWorkgroup and maxComputeWorkgroupSizeX >= 512 (WebGPU default is 256; adapter here reports 1024; Apple GPUs also allow 1024) - fall back to the 256-thread variant (1.23x) if the limit is unavailable.


### 12. Option (b) 'all heads x tiles as more workgroups' cannot add parallelism without a cross-workgroup reduction, which forces 2 dispatches per token with S in global memory; not recommended.

Splitting a head across workgroups by COLUMNS needs no reduction but does not increase total threads (still 6144 column-owners), only changes the WG shape - no gain. Splitting by ROWS does add threads but vhat_j = sum_i S[i,j]k_i needs all rows, so each token needs a global barrier: stage A (per row-tile partial vhat/sq -> global partials) + stage B (reduce, d, S tile update, o) = 2 dispatches x nCols tokens x 48 layers = 384-1536 extra dispatches per pass, with S re-read/written from L2 per token (3 x 64 KB/head-token, decay folded into stage B). At the ~50 ms non-matvec floor already dominated by dispatch count in this engine, this adds overhead rather than removing it. My V5 test variant hit a bind-group/auto-layout mismatch (stage A did not reference all bindings; relDiff 0.37, 1.2 ms timings are from failed dispatches) so it has no valid measurement; the analysis above stands on structure.

_Source:_ scratch/dnbench.js (v5), engine/qwen35.js:1059-1112 (_encodeLayerBatch dispatch list)

_Actionable:_ Skip (b). If you want more than 512-1024 threads per head, do it inside one workgroup (V3/V4), not across workgroups.


### 13. Option (c) workgroup memory: the full 64 KB S does not fit (16-32 KB limit) and does not need to - registers hold it; only ~5 KB of workgroup memory is required for the tiled kernel.

What fits per WG: at 16 KB, a 32x128 f32 strip (or 64x128 in f16); at 32 KB, 64x128 f32. Holding S in shared memory would require splitting each head across 2-4 column-tile workgroups, which gives no extra parallelism (see (b)) and shared memory is slower than registers. With column ownership, the register file is the right home: 128 threads x 128 f32 = 64 KB = exactly one SM's register share per WG on NVIDIA (V1u, 1.14x), or 512 threads x 32 f32 (V3, 1.85x) with much lower per-thread pressure. Workgroup memory is only needed for the k/q staging (2 x 128 x 4 B = 1 KB) and the row-group partials (RG x 128 x 2 x 4 B = 2 KB at 256 threads, 4 KB at 512, 8 KB at 1024), all well under 16 KB, Chrome and Safari safe. Caveat: WGSL has no unroll pragma; a dynamically indexed private array is placed in local memory (V1 dyn-loop showed zero gain), so generate the WGSL with the row loops unrolled (constant indices) as dnbench.js does.

_Source:_ scratch/dnbench.js (v1/v2 generators); adapter limits printed by the bench: maxComputeWorkgroupStorageSize 49152, maxComputeInvocationsPerWorkgroup 1024 on GB10

_Actionable:_ Do not try to tile S through workgroup memory; use unrolled private arrays (registers) + a few KB of workgroup memory for partials.


### 14. Option (d) f16 storage of S is incompatible with the 'exact same final state S for decode' constraint and is unnecessary once S is register-resident.

S is carried across every token of the whole sequence; rounding it to f16 (11-bit mantissa) between passes introduces ~5e-4 relative error per token that compounds through decay/update, far beyond the 1e-7 validation and it would change the greedy token stream on long prompts unless decode also switches to f16 state (a model-quality decision, not an optimization). Traffic argument also vanishes: V0 moves ~2.3 GB/pass of L2-resident S, register variants move 288 MB/pass (~1 ms at 273 GB/s), so halving it buys <0.5 ms. f16 also needs `enable f16;` + the shader-f16 feature (available on Chrome/Safari Apple, but adds a code path).

_Source:_ Analysis; traffic numbers derived from dims in engine/qwen35.js and the dnbench.js timings

_Actionable:_ Reject (d).


### 15. Cheapest path to a real 2-4x prefill gain: fix the batched GEMV column scaling and the dispatch floor, not the recurrence; a V3 dn_delta_mc rewrite is a cheap ~5-8 ms/pass side win.

Budget at 8 columns: 216 ms = 163 ms matvec_b + ~45 ms other kernels/dispatch overhead + ~8 ms dn_delta_mc. If matvec_b at 8 columns were brought to ~1.3x the single-token matvec cost (~105 ms, i.e. actually amortizing the 16 GB weight stream) the 8-col pass drops to ~160 ms = 50 tok/s (1.35x); at 16 columns with the same kernel efficiency ~185 ms = 86 tok/s (2.3x). Concrete matvec_b directions consistent with the existing coopWGSL structure: reduce ROWSB to 1-2 when COLS >= 8 to keep accumulators at <= 32 registers, load x through workgroup memory once per 32-elem block instead of 2*COLS vec4 loads per lane per block, and let the prefill path use a different (COLS, ROWSB) instantiation than verify. Separately, the ~45-50 ms non-matvec residual (~20 dispatches/layer x 64 layers) is a fixed per-pass cost that gets amortized only by larger column counts, which is another reason the GEMV must scale to 8-16 columns first. For the recurrence itself, V3 (512-thread register tiles) is ~150 lines, keeps bind groups/dispatch count, matches to 5e-8, and returns ~4-5 ms of the 8 ms at 8 columns.

_Source:_ breakdown.txt / breakdown8.txt (engine bench), dnbench.js results, engine/engine.js:411-600 (coopWGSL / guKernelB)

_Actionable:_ Priority order: (1) instrument matvec_b at batchCols 8 and 16 (bench_breakdown8.js in scratchpad already does 8) and restructure guKernelB/coop_b for COLS>=8 (row/column tile shape + x staging); (2) cut per-layer dispatch count in _encodeLayerBatch; (3) optionally drop in the V3 dn_delta_mc from dnbench.js with a requestDevice limit bump to 512 invocations/WG and a 256-thread fallback.


### 16. fla (flash-linear-attention) chunked forward = 5 Triton kernels with chunk size BT=64 (16/32 also allowed); parallelism is over (chunk, batch*head) for the intra-chunk kernels and over (V-tile, batch*head) for the single sequential-over-chunks state kernel.

chunk_gated_delta_rule_fwd (chunk.py:33-123) runs: (1) chunk_local_cumsum(g, chunk_size=64, scale=RCP_LN2) -> per-chunk prefix sum of log-gate in fp32, reset every chunk, pre-scaled by 1/ln2 so all decays use exp2; (2) chunk_gated_delta_rule_fwd_intra -> w,u,A (WY representation); (3) chunk_gated_delta_rule_fwd_h -> h (state snapshot at the START of every chunk, [B,NT,HV,K,V]), v_new, final_state; (4) chunk_fwd_o -> o. Grid for (2): (NT, B*HV) -> one program per (chunk, head), fully parallel. Grid for (3): (cdiv(V,BV) * N * HV,) with BV in {32,64}: each program owns a K x BV slice of the state for one head and loops sequentially over all NT chunks. Grid for (4): (cdiv(V,BV), NT, B*HV), fully parallel because it reads the stored per-chunk h. Head broadcast (HV > H, i.e. GQA-like q/k sharing) is handled by indexing k with i_h // (HV//H), never by materializing repeats.

_Source:_ scratch/fla/fla/ops/gated_delta_rule/chunk.py (lines 33-123), chunk_fwd.py:369-428, common/chunk_delta_h.py:687-743, common/chunk_o.py:544-586; github.com/fla-org/flash-linear-attention

_Actionable:_ Map to WGSL as 4 dispatches per layer per 64-token chunk-batch: (a) cumsum+kkt+solve (one workgroup per (chunk, head)), (b) w/u (same grid), (c) state recurrence (one workgroup per (head, V-tile), loop over chunks inside), (d) output (one workgroup per (chunk, head, V-tile)). Only (c) is serial and it is serial over chunks (T/64 steps), not tokens.


### 17. fla intra-chunk: A = strictLower(beta_i * (k_i . k_j) * exp(gcum_i - gcum_j)), and (I+A)^-1 is built by explicit forward substitution on 16x16 diagonal blocks followed by a block-merge with 4 small matmuls; then u = Ainv @ (v*beta), w = Ainv @ (k*beta*exp(gcum)).

chunk_gated_delta_rule_fwd_kkt_solve_kernel (chunk_fwd.py:40-328) for BT=64 uses BC=16 sub-chunks: computes all 10 lower-triangular 16x16 blocks of K K^T in fp32 registers (loop over K in BK=32/64 slabs), multiplies each by exp2(g_i - g_j) masked to i>j (masks also guard 0*inf NaN at sequence boundaries), scales rows by beta, then inverts each diagonal block via row-by-row forward substitution: for i in 2..15: a = -A[i,:i]; a = a + sum_r a[r]*Ainv[r,:]; Ainv[i,:] = a; Ainv += I. Off-diagonal blocks of the inverse: Ainv10 = -(Ainv11 @ A10) @ Ainv00, Ainv20 = -Ainv22 @ (A20@Ainv00 + A21@Ainv10), etc. (SOLVE_TRIL_DOT_PRECISION = tf32 only where supported, else ieee). The unfused path (chunk_scaled_dot_kkt_fwd -> solve_tril) is the same math with the 16x16 solve in solve_tril_16x16_kernel and merge kernels 16->32->64. recompute_w_u_fwd_kernel (wy_fast.py:43-106) grid (NT, B*HV), BK=BV=64: u = dot(A, v*beta) with allow_tf32=False; w = dot(A, k*beta*exp2(gcum)). A is stored in k.dtype (bf16 in training), w,u in k.dtype; accumulation in fp32.

_Source:_ scratch/fla/fla/ops/gated_delta_rule/chunk_fwd.py, wy_fast.py, /tmp/.../fla/fla/ops/utils/solve_tril.py:37-99, common/chunk_scaled_dot_kkt.py

_Actionable:_ For a WGSL port with 16-32KB workgroup memory, the 16x16 block scheme is the right granularity: a 64x64 fp32 A is 16KB; a 16x16 block is 1KB. Forward substitution over 16 rows is a 16-step serial loop where each step is a 16-wide vector op, i.e. trivially done by 16-64 threads with 1 barrier per row. Keep everything fp32 (no tf32/bf16 downcasts) to stay closest to the sequential recurrence.


### 18. fla inter-chunk state recurrence (chunk_gated_delta_rule_fwd_kernel_h_blockdim64): state kept in fp32 registers as 64-row blocks x BV columns; per chunk: v_new = u - w @ h; store h snapshot; h *= exp2(g_last); h += k^T @ (v_new * exp2(g_last - g_t)).

Kernel body (chunk_delta_h.py:58-348): each program owns b_h1..b_h4 (K split into 64-row blocks, so K=128 -> two [64,BV] fp32 register tiles; STATE_V_FIRST variant stores [BV,64]). Loads h0 (initial_state, fp32) once. For i_t in range(NT): (1) store current h to global h[i_t] (bf16/k.dtype) - this is the state at chunk START that chunk_fwd_o reads; (2) b_v = load(u) - dot(w, h) accumulated over the K blocks; store v_new (SAVE_NEW_VALUE); (3) last_idx = min((i_t+1)*BT, T)-1; g_last = g[last_idx]; b_v *= exp2(g_last - g_t) (masked to valid tokens); h *= exp2(g_last); (4) b_v cast to k.dtype; h += dot(k^T[64,BT], b_v[BT,BV]). Final: store final_state fp32 ([N,HV,K,V]). Note ordering: decay of the OLD state by exp2(g_last) and the per-token decay of contributions exp2(g_last - g_t) reproduce exactly S_end = exp(sum g) * S_start + sum_t exp(g_last - gcum_t) k_t v_new_t^T. Autotune: BV in {32,64} (32 only on low-shared-mem GPUs), num_warps 2-4 (2 only on Blackwell due to a Triton race), num_stages 1-4. K <= 256 asserted.

_Source:_ scratch/fla/fla/ops/common/chunk_delta_h.py lines 24-348, 687-743

_Actionable:_ WGSL analogue for dState=128: one workgroup per (head, 32-column V-tile) -> 4 workgroups per head, each with a 128x32 fp32 state slice = 16KB in workgroup memory (fits your 16-32KB budget), 256 threads each owning 16 state elements in registers if you prefer to avoid shared-memory traffic. Per chunk it performs two [64x128]x[128x32] matmul-like passes; k/w/u tiles for the chunk are streamed through a small shared staging buffer (64x128 f32 = 32KB is too big; stage 16 rows at a time = 8KB).


### 19. fla output kernel (chunk_fwd_kernel_o) combines inter-chunk o = (q * exp2(gcum)) @ h_chunkstart with intra-chunk o = mask_{i>=j}((q k^T) * exp2(gcum_i - gcum_j)) @ v_new, both scaled by 1/sqrt(K) at the end; accumulation fp32, inputs may be bf16.

chunk_o.py:56-153: grid (cdiv(V,BV), NT, B*HV); loop over K in BK slabs accumulating b_o += q @ h and b_A += q @ k^T; then b_o *= exp2(g)[:,None]; b_A *= exp2(g_i - g_j); b_A masked to (o_t_i >= o_t_j) & valid; b_o = b_o*scale + dot(b_A.to(v.dtype), v_new)*scale. Configs {BK,BV} = 128/64/32 with 8/4/2 warps. The diagonal (i==j) is INCLUDED in the intra term (mask is >=), whereas the KK^T matrix used for the WY solve is strictly lower (i>j).

_Source:_ scratch/fla/fla/ops/common/chunk_o.py lines 45-153, 544-586

_Actionable:_ Because o for chunk c needs only h at chunk start (already produced by the recurrence kernel), the output pass is embarrassingly parallel across all chunks and heads and can be one big dispatch after the recurrence finishes. Do not fuse it into the sequential recurrence workgroup.


### 20. fla's decode/short-sequence path (fused_recurrent_gated_delta_rule) is token-sequential but splits the state by V-columns into tiny tiles: BV = 8 (or 32 in vLLM's copy), grid = (NV * N * HV) programs each with num_warps=1, each program holding a K x BV fp32 state slice in registers.

fused_recurrent.py:30-175 and wrapper lines 206-247: BK = next_pow2(K) (whole key dim per program), BV = min(8, next_pow2(V)) -> for V=128 that is 16 programs per head, each 32 threads, each thread owning a few state elements. Per token: b_h *= exp(g); b_v = beta*(v - sum(h * k[:,None], 0)); h += k[:,None]*b_v; o = sum(h*q[:,None], 0). In-kernel L2-norm option: q = q / sqrt(sum(q*q)+1e-6) in fp32 (same for k), then q *= scale. All fp32. vLLM's vendored copy uses BV=32 -> grid (NK, NV, N*HV) = 4 programs/head, num_warps=1, and supports ssm_state_indices (paged state cache) so the state is read/written in place.

_Source:_ scratch/fla/fla/ops/gated_delta_rule/fused_recurrent.py; /tmp/.../vllm/vllm/third_party/flash_linear_attention/ops/fused_recurrent.py lines 178-248

_Actionable:_ Even for the sequential kernel, nobody uses 'one workgroup of dState threads per head' with the state in global memory. Split the 128 columns of a head across 4-16 workgroups (each owning 8-32 columns) and split rows across threads with a shared-memory reduction for the two dot products; that multiplies active workgroups by 4-16x with identical arithmetic.


### 21. HuggingFace Qwen3-Next / Qwen3.5 torch fallback (torch_chunk_gated_delta_rule): chunk_size=64, everything cast to fp32, pads T to a multiple of 64, solves the intra-chunk system with torch.linalg.solve_triangular(unitriangular lower) (or explicit forward substitution when exporting), applies all decays once before the chunk loop, and loops sequentially over chunks with 4 matmuls each.

modeling_qwen3_5.py:257-390 (identical in qwen3_next). Steps: transpose to [B,H,T,D], .to(float32); optional l2norm (x * rsqrt(sum(x*x)+1e-6)); q *= K^-0.5; pad; v_beta = v*beta, k_beta = k*beta; reshape to [B,H,NT,64,D]; cum_decay = decay.cumsum(dim=3) (per chunk); pairwise_decay = exp(cum_i - cum_j) with strictly-upper masked to -inf (-> 0); ut_system = (k_beta @ k^T) * pairwise_decay; intra_chunk_attn = (q @ k^T) * pairwise_decay; decayed_k_beta = k_beta * exp(cum_decay); new_values = solve_triangular(ut_system, v_beta, upper=False, unitriangular=True); k_cumdecay = solve_triangular(ut_system, decayed_k_beta, ...). NOTE: ut_system has diagonal = beta*|k|^2 * 1 but unitriangular=True ignores the diagonal, so effectively I + strictLower(A). Then query *= exp(cum_decay); key *= exp(cum_last - cum_decay); chunk_decay = exp(cum_last). Loop i over chunks: v_new = new_values[i] - k_cumdecay[i] @ S; inter = query[i] @ S; out[i] = inter + intra_chunk_attn[i] @ v_new; S = S*chunk_decay[i] + key[i]^T @ v_new. Output cast back to input dtype; final S stays fp32. Dispatch: recurrent (torch_recurrent_gated_delta_rule, per-token loop, fp32) is used only when cache has state AND seq_len == 1; every multi-token call (prefill, and chunked prefill continuing from a cached state via initial_state) uses the chunk function. Older versions clamped (g_cum[-1]-g_cum) at max=50 before exp (llama.cpp comment references it); current code drops the clamp since g<=0 makes all exponents <=0.

_Source:_ src/transformers/models/qwen3_5/modeling_qwen3_5.py lines 250-453, 505-615; github.com/huggingface/transformers

_Actionable:_ This is the cleanest fp32 reference to validate a WGSL chunked port against: it is pure fp32 like your engine, so its output vs. the sequential recurrence tells you the achievable relDiff (expect ~1e-6, not 1e-7). Port its exact formulas: A_ij = beta_i (k_i.k_j) exp(gc_i - gc_j) for i>j, u = (I+A)^-1 (beta v), w = (I+A)^-1 (beta exp(gc) k), q' = q exp(gc), k' = k exp(gc_last - gc), S' = exp(gc_last) S + k'^T (u - w S).


### 22. HF causal conv1d for prefill is NOT a recurrence: causal_conv1d_fn is a plain depthwise F.conv1d with left padding 3 over [conv_state | x], parallel across tokens; only single-token decode uses the stateful causal_conv1d_update. llama.cpp does the same with ggml_ssm_conv over a concatenated [3 past inputs | tokens] buffer.

modeling_qwen3_5.py:205-245 and 530-556: prefill concatenates cached conv state (last 3 inputs) in front of the new tokens via cache_params.update_conv_state, runs F.conv1d(padding=k-1, groups=channels) and takes the last seq_len outputs; new conv state = last 3 inputs. llama.cpp delta-net-base.cpp:449-525 build_conv_state: conv_input = concat(conv_states[3, C], qkv_mixed^T) then ggml_ssm_conv + silu; the new state is a view of the last 3 columns copied back. Output token t depends only on x[t-3..t], never on earlier outputs.

_Source:_ src/transformers/models/qwen3_5/modeling_qwen3_5.py:205-245,530-556; llama.cpp/src/models/delta-net-base.cpp:449-525; llama.cpp/src/models/qwen35.cpp:382-399

_Actionable:_ Your dn_conv_mc (qwen35.js:248-270) loops columns serially inside each channel thread with a carried s0..s2 shift register. Rewrite it as a 2-D dispatch over (channel, column): read x[col-3..col] from the batch buffer (falling back to convState for col<3), compute silu, and have only the last 3 columns write the new state. That removes the serial column loop entirely; math is bit-identical.


### 23. llama.cpp has BOTH a graph-level chunked DeltaNet (CS=64, CS=16 for KDA) and a fused single-op token-sequential kernel (GGML_OP_GATED_DELTA_NET); as of this checkout the fused sequential op is the DEFAULT for multi-token prefill too (fused_gdn_ch=true) on every backend that implements it, and the chunked graph is only the fallback.

llama-context.cpp:232-233 sets cparams.fused_gdn_ar = fused_gdn_ch = true; lines 559-563 probe whether the backend assigned to each layer supports the fused op and disable otherwise. delta-net-base.cpp:425-447 build_delta_net: n_tokens==1 -> fused AR op (or build_delta_net_autoregressive graph); n_tokens>1 -> fused op (LLM_FUSED_OP_GDN_CH) if enabled, else build_delta_net_chunking. The chunked graph (lines 16-287) is the HF algorithm expressed in ggml ops: pad to CS, g_cs = cumsum per chunk, decay_mask = exp(tri_lower(g_j - g_i)), kb = (k @ k_beta) * mask, kq = (k @ q) * mask, lhs = tri_lower(kb)+I, attn = solve_tri(lhs, -tri_lower(kb)) + I (this IS (I+A)^-1 materialized), v = v_beta^T @ attn, k_cd = (k_beta*exp(g_cs)) @ attn, q_g_exp = q*exp(g_cs), kg = k*exp(g_last - g_cs), then a per-chunk loop of 5 mul_mats + state = state*exp(g_last) + kg^T @ v_new. All fp32. The PR that added the fused op (#19504) states it is 'a basic vector impl and not the chunking impl', that reviewers noted 'processing a few tokens auto-regressively (more than 1 and less than ~16) would be more efficient compared to the chunking path', and lists a 'TODO: Add chunked kernel for even faster pre-fill' in gated_delta_net.cu:180. Reported gains from the fused op were decode-side (RTX 5090 tg32 83.9 -> 105.9 t/s; M4 Max 0.8B 170 -> 213 t/s); PR #19375 (graph restructuring) reported M2 Ultra 80B-A3B Q4_0 pp512 930 -> 1125 t/s, pp1 37.9 -> 52 t/s.

_Source:_ llama.cpp/src/models/delta-net-base.cpp, llama.cpp/src/llama-context.cpp:232-233,555-563, llama.cpp/ggml/src/ggml-cuda/gated_delta_net.cu:180; https://github.com/ggml-org/llama.cpp/pull/19504 ; https://github.com/ggml-org/llama.cpp/pull/19375

_Actionable:_ Two lessons: (1) with only 4-8 tokens per pass, a chunked algorithm will not beat a well-parallelized sequential kernel (llama.cpp's own reviewers put the crossover around 16 tokens), so first fix the sequential kernel's occupancy and memory traffic; (2) the chunked path only pays once you feed 64 tokens per pass, which also means turning your 4/8-column GEMVs into 64-column GEMMs - the two changes go together.


### 24. llama.cpp fused sequential kernel thread layouts: CUDA/Vulkan/Metal split EACH state column across a full 32-lane warp (each lane holds S_v/32 = 4 rows of the column in registers) and use warp/simd reductions for the two dot products, giving 32x more threads per head than one-thread-per-column; the WebGPU (WGSL, no subgroups) port uses one 128-thread workgroup per (head, seq) with the whole column in a private register array and q/k staged in workgroup memory.

CUDA (gated_delta_net.cu:4-221): grid (H, n_seqs, S_v/4), block (32, 4) -> 4 warps/block, one column per warp, 32 blocks per head for S_v=128, __launch_bounds__(128, 2). Per token: load k,q into k_reg/q_reg[4]; kv_shard = sum s_shard*k_reg -> warp_reduce_sum; delta = (v[col] - g*kv)*beta; s_shard = g*s_shard + k_reg*delta and attn_partial += s_shard*q_reg -> warp_reduce_sum; lane 0 writes o[col]*scale. State never touches global memory until the end (or per-token snapshot slots when K>1). Metal (ggml-metal.metal:2701-2832): threadgroups (S_v/nsg, H, n_seqs) x threads (32, nsg): each 32-lane simdgroup owns one column with NSG=S_v/32 rows per lane, simd_sum reductions. Vulkan (gated_delta_net.comp): spec constants LANES_PER_COLUMN (default 32), COLS_PER_WG = SUBGROUP_SIZE/LANES_PER_COLUMN, ROWS_PER_LANE = S_V/LANES_PER_COLUMN, uses subgroupClusteredAdd/subgroupAdd, and includes a shared-memory butterfly fallback reduce_add_shmem() (temp[SUBGROUP_SIZE], log2 steps with barriers) for devices without subgroup arithmetic. WebGPU (gated_delta_net.wgsl + shader-lib.hpp:1884-1885): defines S_V=WG_SIZE=S_v, grid (H, n_seqs); each thread owns column col: 'var state: array<f32,S_V>' loaded once; per token sh_q[col]=q[col], sh_k[col]=k[col] into var<workgroup>, barrier, then the 128-iteration loops read only workgroup memory + registers; state written to global only at the end. CPU (ops.cpp:10756-10907): threads over (head, seq) rows, state stored transposed so column j is contiguous, ggml_vec_scale/dot/mad SIMD; per token 4 passes over the 128x128 state.

_Source:_ llama.cpp/ggml/src/ggml-cuda/gated_delta_net.cu; llama.cpp/ggml/src/ggml-metal/ggml-metal.metal:2696-2832; llama.cpp/ggml/src/ggml-metal/ggml-metal-ops.cpp:1886-1888; llama.cpp/ggml/src/ggml-vulkan/vulkan-shaders/gated_delta_net.comp; llama.cpp/ggml/src/ggml-webgpu/wgsl-shaders/gated_delta_net.wgsl; llama.cpp/ggml/src/ggml-webgpu/ggml-webgpu-shader-lib.hpp:1860-1888; llama.cpp/ggml/src/ggml-cpu/ops.cpp:10756-10949

_Actionable:_ Your dn_delta_mc (qwen35.js:294-333) differs from every one of these in one costly way: the state lives in global storage (dlm_s) and every token does two full read-modify-write sweeps of the 128x128 f32 state per head plus 3 global k/q reads per row. Adopt the WebGPU-shader pattern first (column in a private array, q/k staged in var<workgroup>, write S back once per pass) - same arithmetic, so it stays bit-identical - then adopt the Vulkan fallback pattern (LANES_PER_COLUMN=4..8 with a shared-memory reduction) to raise occupancy from nVH*128 threads to nVH*512..1024.


### 25. llama.cpp's fused kernels use a single-pass update order that avoids a separate decay sweep: kv = S^T k on the UN-decayed state, delta = (v - exp(g)*kv)*beta, then S = exp(g)*S + k*delta and o = (S^T q)*scale, fused in one loop.

CUDA lines 84-110, WGSL lines 113-124, Metal 2764-2796 (Metal decays first inside the same loop: ls *= g_exp; s_k += ls*k). Mathematically identical to S=S*decay; vhat=S^T k; d=(v-vhat)*beta; S+=k d; o=S^T q. Scale 1/sqrt(S_v) is applied to the output, not to q (equivalent in exact arithmetic; fla/HF scale q instead). All backends are strictly fp32 (op asserts F32 for q,k,v,g,beta,state). The op also supports K>1 'snapshot slots' that dump the state after each of the last K tokens for speculative-decoding rollback - the same idea as your dlm_shadow.

_Source:_ llama.cpp/ggml/src/ggml-cuda/gated_delta_net.cu:84-110; llama.cpp/ggml/src/ggml-webgpu/wgsl-shaders/gated_delta_net.wgsl:113-127; llama.cpp/ggml/src/ggml.c:6278-6331

_Actionable:_ Your kernel already fuses vhat/sq/kq into one sweep and computes o = (sq + d*kq)*scale, which is one sweep fewer than llama.cpp's; keep that trick, just move the state out of global memory. The tiny rounding differences between the two orderings are ~1e-7 relative, well inside your validation threshold.


### 26. vLLM prefill for Qwen3-Next/Qwen3.5 GDN uses a chunked kernel with BT=64 on all three of its GPU backends (vendored fla Triton, FlashInfer, and a CuTeDSL Blackwell path), plus a chunked C++ op on CPU; decode uses fla's fused_recurrent (or a packed-decode variant) reading/writing a paged state cache in place via ssm_state_indices; the state cache dtype is fp32 or bf16.

qwen_gdn_linear_attn.py: FLA_CHUNK_SIZE=64 (third_party/.../ops/utils.py:31); prefill runs fused_post_conv_prep (one Triton kernel fusing split + rearrange + L2-norm of q,k + gating g = -exp(A_log)*softplus(a+dt_bias), beta = sigmoid(b), writing g/beta as fp32) then ChunkGatedDeltaRule which dispatches to: forward_native -> vendored fla chunk_gated_delta_rule (cumsum -> chunk_scaled_dot_kkt_fwd (fp32 A) -> solve_tril (output k.dtype) -> recompute_w_u_fwd -> chunk_gated_delta_rule_fwd_h (grid (V/BV, N*H), state as [BV,64] fp32 blocks, V-first layout [V,K]) -> chunk_fwd_o writing directly into core_attn_out); forward_cuda -> FlashInfer chunk_gated_delta_rule with state cast to fp32; forward_cutedsl -> 3 CuTeDSL kernels (kkt_inv_uw: Gamma=exp(gc_i-gc_j), A=strictLower(beta*KK^T*Gamma), Ai=inv(I+A), U=(Ai*beta)@V, W=(Ai*beta*exp(gc))@K with 12 warps; h kernel: V_new = U - W@H^T, H = H*exp(g_last) + (V_new*exp(g_last-g))^T@K with 10 warps, tensor-core bf16 inputs, H accumulated fp32 (h_dtype Float32 or BFloat16); o kernel: O = scale*(exp(gc)*(Q@H^T) + mask((Q@K^T)*Gamma)@V)); all hardcode BT=64 and K=V=128. The vendored chunk asserts q.dtype != float32 ('Please use bfloat16'). Decode: fused_recurrent_gated_delta_rule_fwd with BV=32, num_warps=1, grid (NK,NV,N*HV), IS_CONTINUOUS_BATCHING via ssm_state_indices; states for sequences without prior state are zeroed then passed as initial_state. CPU: ops.chunk_gated_delta_rule_cpu(..., cu_seqlens, initial_state_indices, use_qk_l2norm_in_kernel=True) - a chunked C++ kernel that gathers/mutates the state pool in place.

_Source:_ scratch/vllm/vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py (lines 90-360, 1259-1470); vllm/third_party/flash_linear_attention/ops/{chunk.py,chunk_delta_h.py,fused_recurrent.py,fused_gdn_prefill_post_conv.py,utils.py}; vllm/model_executor/layers/mamba/ops/gdn_chunk_cutedsl/{__init__.py,kernel_h.py,kernel_kkt_inv_uw.py,kernel_o.py}; vllm/model_executor/layers/mamba/ops/cpu/gdn_attention.py:236-262

_Actionable:_ vLLM confirms the production recipe: chunked (BT=64) for any multi-token prefill, sequential fused_recurrent only for 1-token decode, both continuing from an explicit initial_state and emitting the final state in fp32. Also worth copying: fuse L2-norm(q,k) + gating (g, beta) + q/k/v de-interleave into one small kernel (your dn_gates_mc + dn_l2_mc + part of dn_delta_mc) - dn_l2_mc currently uses 32-thread workgroups with a 2x128 serial loop per head.


### 27. Precision across implementations: the running state is fp32 everywhere (fla, HF, llama.cpp, vLLM) and the final state is emitted fp32; fla/vLLM tolerate bf16 for A, w, u, v_new and per-chunk h snapshots (tensor-core inputs) but accumulate in fp32; HF and llama.cpp are fp32 end-to-end; gate cumsums are always fp32; decay factors are always computed as exp of differences of cumulative log-gates (all exponents <= 0 because g <= 0), never as products of per-token decays.

fla: chunk_local_cumsum output_dtype=torch.float (fp32) with scale=RCP_LN2=1.4426950216 so kernels use exp2; l2norm_fwd in fp32 with eps=1e-6 inside the sqrt (x / sqrt(sum x^2 + eps)) - HF's l2norm uses rsqrt(sum+eps), your dn_l2_mc uses 1/max(sqrt(ss), eps2), a slightly different eps semantics; fla stores A, w, u in k.dtype and h in k.dtype but b_h registers and final_state are fp32; solve_tril stores with fp_downcast_rounding='rtne'; dot(A, v*beta) uses allow_tf32=False and solve_tril precision defaults to 'ieee' (env FLA_TRIL_PRECISION). HF: .to(torch.float32) on all inputs, fp32 solve_triangular. llama.cpp: op asserts GGML_TYPE_F32 on all six inputs; graph path fp32 with ggml_solve_tri. vLLM CuTeDSL: bf16 MMA inputs, fp32 accumulators, H fp32 (optionally bf16 cache). None of the fp32 implementations claim bitwise equality between chunked and sequential; fla's own naive_chunk_gated_delta_rule reference (naive.py:65-161) is used with tolerance tests.

_Source:_ scratch/fla/fla/{ops/utils/constant.py,ops/utils/cumsum.py,modules/l2norm.py,ops/gated_delta_rule/naive.py}; HF modeling_qwen3_5.py:250-304; llama.cpp/ggml/src/ggml.c:6294-6299

_Actionable:_ For a WGSL chunked port keep everything f32 and use exp() of cumulative-gate differences exactly as HF does; expect the greedy token stream to match but relDiff vs the sequential path to rise from ~1e-7 to ~1e-6..1e-5 (error grows with |A| entries, i.e. large beta and near-parallel keys inside a chunk). If you want the smallest deviation, use BT=16 (allowed by fla; llama.cpp uses 16 for KDA): the 16x16 forward substitution is exact-order-friendly, the WY matmuls are short, and inter-chunk recurrence still runs 16x fewer steps than token-serial.


### 28. A concrete WGSL plan derived from these implementations, ordered by expected payoff for your 4/8-column passes today and a future 64-column pass.

Step 1 (no algorithm change, bit-identical): in dn_delta_mc keep the column S[:,j] in a private array (128 f32) for the whole column loop, stage q/k for the current column into var<workgroup> (2 x 128 f32 = 1KB) with one barrier, read v[j]/beta/decay once, write S back to dlm_s once at the end (and to dlm_shadow only for snapshot columns) - this is exactly llama.cpp's gated_delta_net.wgsl. Step 2 (bit-close, changes reduction order): split each column across R=4 or 8 threads (workgroup 512/1024 = 128 columns x R lanes, or 4-8 workgroups per head of 128 threads each owning 16-32 columns x R lanes), each lane holding 128/R state rows in registers; the two per-token dot products (vhat and sq/kq) need one shared-memory tree reduction each (Vulkan reduce_add_shmem pattern: R-wide butterfly, log2(R) barriers); this raises active threads per head from 128 to 512-1024 and shortens the dependent FMA chain per token by R. Also parallelize dn_conv_mc over (channel, column) since conv is not a recurrence. Step 3 (chunked, for >=16-64 tokens/pass): per layer dispatch (a) cumsum g per chunk + KK^T + 16x16 forward-substitution + block merge -> (I+A)^-1 [64x64 f32 = 16KB workgroup memory] per (chunk, head); (b) u = Ainv(beta v), w = Ainv(beta exp(gc) k) per (chunk, head) [64x128 outputs]; (c) sequential-over-chunks state kernel per (head, 32-column V-tile) holding a 128x32 f32 state slice (16KB) and doing v_new = u - w S_tile, S_tile = exp(gc_last) S_tile + (k * exp(gc_last - gc))^T v_new, also writing S at chunk start to a per-chunk h buffer; (d) fully parallel output kernel per (chunk, head, V-tile): o = exp(gc) q h_start + mask(q k^T exp(gc_i-gc_j)) v_new, times 1/sqrt(128). Final S after the last chunk is the exact state decode needs (all implementations emit it from the same recurrence).

_Source:_ Synthesis of: llama.cpp/ggml/src/ggml-webgpu/wgsl-shaders/gated_delta_net.wgsl, llama.cpp/ggml/src/ggml-vulkan/vulkan-shaders/gated_delta_net.comp:45-93, fla common/chunk_delta_h.py, HF modeling_qwen3_5.py:257-390, and your engine/qwen35.js:248-333

_Actionable:_ Do Step 1 immediately (small diff, validates against the existing sequential golden at relDiff ~1e-7), measure, then Step 2. Only take on Step 3 together with widening the prefill pass to 64 columns, since below ~16 tokens per pass the chunked WY machinery costs more than it saves (llama.cpp reviewers' and fla's own guidance).


### 29. The premise is wrong on this GPU: the batched prefill pass is bound by the per-column cost of the batched GEMV kernels (matvec_*_coop_b), not by the DeltaNet recurrence. Measured in isolation on the GB10 (all 64 layers, one command buffer, repeated): matvec_b = 91 ms @2 cols, 108 ms @4, 170 ms @8, 401 ms @16, while the whole batched pass is 131 ms @4, 210 ms @8, 459 ms @16 and dn_delta_mc is only 5.6/9.2/14.1 ms @1/4/8 cols.

Weight-streaming floor for the ~13.8 GB of Q4_0 layer weights is ~50-55 ms at 273 GB/s, so the batched matvec is at 2x the floor already at 2 columns and grows ~8 ms/col (2->4), ~16 ms/col (4->8), ~29 ms/col (8->16): superlinear, consistent with register pressure (COLS*2 vec4 of x + ROWSB*COLS accumulators per thread) plus ALU/issue cost that is not hidden under DRAM time (the kernel reaches only ~0.9 T MAC/s per column = ~8% of f32 FMA peak). Changing ROWSB (2/4/8) and WGB (64/128/256) did not help (bench_deno prefill 29-31 tok/s at 8 cols for ROWSB 2/4, 27 for ROWSB 8; matvec_b 172 ms at WGB 128 and 256), so it is not simply an x-traffic or occupancy-by-workgroup-size problem. In tok/s terms the current kernel peaks at 8 columns (38 tok/s pass-only) and gets worse at 16 (35 tok/s). The 4-vs-8 'same 27 tok/s' observation in bench_deno is largely the host loop: the <NC leftover prompt tokens run as single-token passes (~100-113 ms each) plus the serial mtpRun per column and a separate forwardToken for the last token, which eats the batched gain. NOTE: batchCols=1 is a silent no-op: coopWGSL generates `Array.from({length: COLS/2})` reduction rounds, which is 0 rounds for COLS=1, so the _b kernels never store y (the 37 ms 'NC=1' figure is a dead-code kernel, do not trust it). Also note engine.js/qwen35.js were being edited concurrently (a matvec_*_coop_acc variant appeared at ~00:02) and another process (test_mtp_deno.js / bench_breakdown_deno.js) shared the GPU for part of the runs; the numbers are consistent across repeats (single pass 112.7-113.0 ms, 8-col pass 208-216 ms) so contention did not distort them much.

_Source:_ scratch/micro.txt, breakdown.txt, breakdown8.txt, rows.txt, wg/out.txt, wg/out2.txt (scripts: scratchpad/micro.js, scratchpad/wg/mv.js); kernel: engine/engine.js lines 748-822 (coopWGSL batched variant), host loop: engine/qwen35.js lines 1288-1314 (prefillTokens).

_Actionable:_ Re-prioritise: (1) treat the batched GEMV as a small-N GEMM problem: stage the COLS x-columns for a dIn chunk in workgroup memory (8 cols x 256 elems x 4 B = 8 KB, under the 16 KB limit) so per-thread register footprint stops growing with COLS, keep ROWSB rows x COLS cols of accumulators, fold the '-8' of Q4 into a per-block precomputed sum(x) so the inner loop is convert+fma only, use explicit fma(), unroll 2 blocks per iteration for more loads in flight, and measure GB/s vs COLS on a synthetic 1 GB Q4 matrix (target: flat ~55-60 ms/pass up to 16 columns, i.e. >150 tok/s prefill); (2) host: run the prompt tail as ONE batched pass with nCols<NC (embedRunBatch already supports it) and take the last prompt token's logits from headBatch on its column instead of a separate forwardToken; batch the MTP fill (the MTP block is one layer, reuse _encodeLayerBatch machinery with x columns shifted by one) instead of NC serial mtpRun submits; (3) only after that do the recurrence/attention items below matter (they are ~25 ms of a 210 ms pass at basePos ~100).


### 30. dn_delta_mc costs ~4.4 ms fixed + ~1.2 ms per column per pass (48 layers), i.e. ~25 us per layer per token; it is latency-bound (48 workgroups x 128 threads, each token = 2 passes of 128 dependent global load->store round trips on S), and the cheapest, bit-identical fix is to keep each thread's state column S[:,j] in registers/private memory for the whole column loop, staging q/k of all columns in workgroup memory. Chunkwise (WY) Gated DeltaNet is NOT worth it at C = 4..16.

Per-token math in dn_delta_mc (qwen35.js 295-333): thread j owns column j of S[h] (dState x dState = 128x128 f32 = 64 KB per head, 3 MB per layer). Per token it reads+writes S once in the decay/vhat loop and again in the rank-1 update loop, each a 128-iteration loop with a dynamic bound (dState from a uniform, so not unrolled), so every iteration stalls on a global load: 2 x 128 x ~0.5-1 us chains -> tens of us per token per layer, matching the measured 25 us/layer/token. S traffic itself is small (3 MB x 4 traversals x 48 layers = 576 MB/token -> ~2 ms at 273 GB/s), so it is latency, not bandwidth. Register-resident version: `var s: array<f32,128>` per thread loaded once, `for col: ...` loop over nCols doing decay, vhat/sq/kq reductions and the rank-1 update entirely in registers, S stored once at the end (and to the shadow slot when frame.snap says so). With dState as a literal 128 (or a pipeline override constant) the loops fully unroll and the array stays in registers (~150 regs/thread; occupancy is irrelevant here because there are only 48 workgroups). Same operations in the same order -> bit-identical to today (it satisfies the 1e-7 validation exactly, unlike a chunkwise algorithm). Also stage q_h/k_h for all nCols into workgroup memory (nCols x 2 x 128 x 4 B = 8 KB at 8 cols) so the 256 per-token broadcast reads come from shared memory, and fold dn_l2_mc (2.9 ms/pass, latency-bound 32-thread workgroups doing 128-long serial loops) into that staging step: compute the L2 sums with a 128-thread tree reduction (rounding differs at ~1e-7) or, to stay bit-identical, let thread 0 do the same serial 128-add sum from shared memory (~0.5 us). Expected: dn_delta+l2 from ~17 ms @8 cols to ~3-5 ms, nearly independent of nCols up to 64, and decode gains ~4 ms/token too. Chunkwise algorithm, in the engine's conventions (S is [dk x dv], S_t = a_t (I - b_t k_t k_t^T) S_{t-1} + b_t k_t v_t^T, o_t = S_t^T q_t * scale): for a chunk of C tokens with cumulative decay g_t = prod_{s<=t} a_s, solve the lower-triangular system (I + L) U' = B, L_ts = b_t (g_t/g_s)(k_s . k_t) for s<t, B_t = b_t v_t - b_t g_t (S_0^T k_t) -> equivalently U' = U - W S_0 with U = T (b .* V), W = T (b .* g .* K), T = (I+L)^-1 (this is exactly FLA's wy_fast `A = -(beta k k^T .* exp(g_s - g_t))` strictly-lower, solve, `w = A (k beta exp g)`, `u = A (v beta)`, then chunk_delta_h `v_new = u - w @ h`); final state S_C = g_C S_0 + K^T diag(g_C/g_t) U' (FLA: `h = h*exp(g_last) + k^T (v_new .* exp(g_last - g))`), outputs O = (g .* Q) S_0 + (causal-masked (Q K^T) .* g_t/g_s) U'. FLOPs per head per chunk are the same as the sequential scan (~C x 3 x 128^2); the only win is token-parallelism, which the register-resident scan does not need at C<=16 (its per-token dependent chain is ~128 FMAs ~0.3 us, so 8 tokens x 48 layers ~ 0.2 ms). It only pays at C ~ 64 (FLA's BT) and would change rounding (triangular solve, different summation order) so the 1e-7 check would become a greedy-stream check. Not recommended now.

_Source:_ Measurements: scratchpad/micro.txt (dn_delta_mc 5.59/9.24/14.13 ms at nCols 1/4/8; dn_l2_mc 2.81-2.95 ms). Kernel: engine/qwen35.js lines 286-333 (dn_delta_mc), 272-284 (dn_l2_mc), dispatch at 1095-1097. Chunkwise math: Yang et al., Gated Delta Networks (arXiv 2412.06464, WY representation, chunkwise algorithm); fla-org/flash-linear-attention fla/ops/gated_delta_rule/chunk.py, wy_fast.py, fla/ops/common/chunk_delta_h.py (chunk_size=64, v_new = u - w@h, h = h*exp2(g_last) + k^T v_new).

_Actionable:_ Implement dn_delta_mc v2: (a) literal/override dState=128 so loops unroll; (b) `var s: array<f32,128>` loaded once from dlm_s, written once (plus shadow slots per column when frame.snap != 0); (c) workgroup arrays qs[nColsMax][128], ks[nColsMax][128] filled cooperatively (128 threads, one float each per column), with the L2 normalisation done there (serial by one thread if you want bit-identity with dn_l2_mc, else a tree); (d) keep the exact per-token statement order (sdec = s*decay; vhat += sdec*ki; sq += sdec*qi; kq += ki*qi; d = (v - vhat)*beta; s += ki*d; o = (sq + d*kq)*scale). Validate with the existing prefill-vs-sequential relDiff check (should be exactly 0 or FMA-contraction-level). Reuse the same kernel with nCols=1 for decode. Skip chunkwise/WY unless you later move to 64-token chunks.


### 31. Per-column full-attention over the KV cache is a real and context-dependent cost: for 16 attention layers at 8 columns it is 7.9 ms at basePos=0, 13.4 ms at 64, 29.6 ms at 256 and 50.5 ms at 500 (vs an ideal K/V read of ~2 ms), dominated by attn_softmax (workgroup_size(1), 24 threads per column, three serial passes over seqLen) and attn_out (6144 threads each looping serially over seqLen). A 'column-parallel with causal mask' version is cheap to add (~60 lines WGSL + 3 bind groups) because the K/V of all columns are already copied into the cache before the attention pass, so the mask is just a per-column length.

Today (_encodeLayerBatch, qwen35.js 1073-1084): K/V for all nCols are copied into kCache/vCache first, then for each column c the trio attn_scores/attn_softmax/attn_out is dispatched with frame.seqLen = basePos+c+1 sharing ONE scores scratch, so columns serialise (3*nCols dispatches per layer, 384 per pass at 8 cols). Measured per-kernel at 8 cols/16 layers, basePos 500: scores 9.9 ms, softmax 20.3 ms, out 22.3 ms. Column-parallel version: dispatch once with gid.y = column, `let len = frame.pos + gid.y + 1u` (bind bgCommonB[0], whose frame.pos = basePos) as the causal bound, q/attnOut addressed with the MC column strides (same MC uniform pattern as rmsnorm_mc), scores scratch sized nCols x nH x maxSeq floats (8x24x512x4 = 393 KB) indexed by (gid.y*nH + h)*maxSeq + t. Correctness: column c must not see keys > basePos+c even though they are already in the cache: the per-column length gives exactly today's semantics, results identical to the serial version (same math per column). Expected gain: the three kernels are latency-bound, so running 8 columns concurrently overlaps them: ~50 ms -> ~12-15 ms at 500, ~13 -> ~5 ms at 64. Second step (still simple, ~1e-7 rounding change from summation order): attn_softmax_mc as one 256-thread workgroup per (head, column) with shared-memory max/sum reductions instead of workgroup_size(1) (also use it for decode: decode softmax alone is 1.3-3.5 ms/token at 16 layers). Third step, the proper kernel (WebLLM/TVM style, adapted to f32 + no subgroups + 16 KB): one workgroup per (kvHead g, column c[, kv-split s]): 256 threads; workgroup memory: Qs[6][256] (6 KB, the 6 GQA query heads of that kv head, heads 6g..6g+5 are contiguous in q), tile[8][256] (8 KB, reused for K then V), scores p[6][8], m[6], d[6]; loop over key tiles of 8 (coalesced loads: consecutive threads read consecutive vec4 of the 1 KB K row), compute 48 scores (6 heads x 8 keys, 4 threads per dot with a 4-way shared reduction), online-softmax update (m_new = max(m, s); d = d*exp(m-m_new) + sum exp(s-m_new); rescale O), reload tile with V, each thread accumulates 6 of the 1536 (head,dim) outputs, finally O/d -> attnOut column. With split-K over the key range (write partial m,d,O; tiny merge kernel) you get nKV x nCols x splits workgroups (e.g. 4x8x4 = 128). Estimated ~3-5 ms per pass at 500 keys for 16 layers (vs 50 today), i.e. at or near the K/V bandwidth floor. For reference TVM/MLC's WebGPU prefill kernel uses bdx=32, num_warps=4 (128 threads), tile_x = tile_z = 16 query/key rows for d=256 f16 (tile_z=8, num_warps=2 when d>=512), Q/K/V tiles in shared memory, exp2-based online softmax (m_smem/d_smem), causal mask `col < kv_len - qo_len + row + 1`; its decode kernel puts all GQA heads of one kv head in one block (bdy = group size, bdx = D/VEC_SIZE, bdz threads split the KV length and merge partial (m,d,O) in shared memory). LlamaWeb (arXiv 2605.20706) reports the same structure (multiple query rows per workgroup, Q/K/V tiles staged in workgroup memory, online softmax in-kernel) and notes attention is 4.4% of a 512-token prefill rising to 14% at 2048 cache, i.e. after the GEMM is fixed attention is second-order but not negligible.

_Source:_ Measurements: scratchpad/micro.txt (attention rows). Code: engine/engine.js lines 321-376 (attn_scores/attn_softmax/attn_out), engine/qwen35.js lines 1018-1028 (per-column bind groups), 1073-1089 (KV copies + serial column loop). TVM: python/tvm/relax/frontend/nn/llm/_kernel_common.py (_get_prefill_kernel_config, _causal_mask), _prefill_kernels.py (_attention_prefill), _decode_kernels.py (_attention_decode). LlamaWeb: arxiv.org/html/2605.20706v1.

_Actionable:_ Do step 1 now (attn_scores_mc/attn_softmax_mc/attn_out_mc with gid.y = column and len = frame.pos + gid.y + 1; per-column scores scratch; 3 dispatches per layer instead of 3*nCols); do step 2 (256-thread softmax per (head,column)) in the same change since it is ~20 lines and helps decode; schedule the fused online-softmax kernel (step 3) after the GEMM work, since at basePos<=128 attention is <15 ms of the pass. Keep the two-pass exact softmax if you want bit-identity with today's decode path for validation; online softmax differs at ~1e-7.


### 32. Other per-column-serial or latency-bound ops in the batched pass (measured per pass, 8 columns): head_norm_mc 3.4 ms (16 layers, constant in nCols), dn_l2_mc 2.9 ms, rmsnorm_mc+add_res_mc 4.5 ms (256 tiny dispatches), dn_gates_mc 1.5 ms (48 dispatches of 48 threads), dn_conv_mc 1.7 ms, plus host-side serial items: 2*nCols copyBufferToBuffer per attention layer, silu_mul per column when the fused gate/up op is absent, the MTP fill (nCols serial mtpRun single-token submits per batched pass), and the <NC prompt tail run as single-token passes.

head_norm_mc (qwen35.js 404-416): 32-thread workgroups, each thread does two serial 256-element loops over global memory -> ~0.2 ms per layer regardless of columns; same shape problem as dn_l2_mc. dn_conv_mc loops over columns in-kernel but the body is 4 FMAs on register-carried state, so its serial column loop is harmless (1.5-1.7 ms total, mostly dispatch + convDim x nCols loads); it could be made column-parallel (y[col] = w3 x[col] + w2 x[col-1] + w1 x[col-2] + w0 x[col-3] with x[-k] from the carried state, state = last 3 inputs) but the gain is nil. Dispatch overhead on this Dawn/Vulkan stack is ~10-20 us per dispatch (rmsnorm_mc: 128 dispatches ~2.2 ms), and a batched pass issues ~1100 dispatches at 8 cols (48x12 + 16x(9+3*nCols)), i.e. ~10-20 ms of pure launch cost. The skip-family method in bench_breakdown_deno.js is too noisy for families under ~10 ms (it produced negative costs and a -49 ms artefact for qsplit/head_norm/rope/sigmoid), so use the isolated-timing approach in scratchpad/micro.js for these. K/V cache writes: 2*nCols*16 = 256 copy commands per pass at 8 cols; small but they sit between compute passes and force pass boundaries.

_Source:_ scratchpad/micro.txt; engine/qwen35.js lines 242-284 (dn_conv_mc, dn_l2_mc), 404-416 (head_norm_mc), 1059-1115 (_encodeLayerBatch: KV copies at 1073-1076, per-column silu at 1109), 1302-1303 (serial mtpRun per column), 1308-1312 (single-token tail); bench_breakdown_deno.js skip method.

_Actionable:_ Cheap wins in rough priority: (1) prompt tail as one nCols<NC batched pass + logits from headBatch (saves up to (NC-1) x ~110 ms per prompt); (2) head_norm_mc and dn_l2_mc -> one workgroup per (head, column) with a shared-memory reduction (or fold l2 into the delta kernel and head_norm into qsplit/rope), ~5 ms/pass and ~5 ms/decode-token; (3) fuse add_res into the following rmsnorm (x += tmp then normalise) and the K/V copy into the k/v GEMV epilogue or one kv_append_mc kernel (write B.k/B.v straight into the cache row basePos+c), cutting ~300 commands per pass; (4) batched MTP fill; (5) leave dn_conv_mc alone. Also fix the COLS=1 dead-store in coopWGSL (guard batchCols >= 2 or make the reduction round count ceil(COLS/2)).


## Verifier corrections

- keep=True — Minor nuances only (claim stands): (1) fla's BV=8 applies only when gv is None; with per-channel gv, BV=next_pow2(V) so one program covers the full head. (2) vLLM's state tile is always [BV, BK] (V-first, L102), i.e. a BV x K slice rather than K x BV; fp32 is the in-register compute dtype, while the paged cache is stored in initial_state's dtype via .to(p_ht.dtype.element_ty). (3) "A few state elements per thread" understates it: K=128,BV=8 -> 1024 fp32/32 threads = 32 per thread (fla); K=128,BV=32 -> 128 per thread (vLLM). (4) vLLM also ships a separate single-token packed decode kernel (fused_recurrent_gated_delta_rule_packed_decode_kernel, L255-485) with the same tiling (BV=min(next_pow2(V),32), num_warps=1, grid (NV, B*HV) or (NV, HV, B) when B*HV>65535) that additionally fuses -exp(A_log)*softplus(a+dt_bias) and sigmoid(b) in-kernel and reads packed mixed_qkv.
- keep=True — Minor precision only: (1) BV list is `[32, 64] if check_shared_mem('ada') else [32]` — i.e. BV=32 is always a candidate and BV=64 is what gets dropped on low-shared-mem GPUs (the phrase "32 only on low-shared-mem GPUs" reads backwards). (2) num_warps: Blackwell [2], Intel [2,4,8,16], all other GPUs [2,4]. (3) num_stages: {2,3,4} with Ampere-class shared mem, else {1,2}. (4) final_state is [N,HV,V,K] when state_v_first=True. (5) The formula is in log2 space in the kernel (exp2 of chunk-local cumsum gates), not natural exp — equivalent given the cumsum's scaling. (6) h0 is loaded and cast to fp32 regardless of its storage dtype.
- keep=True — (1) HF caches the last conv_kernel_size = 4 inputs, not 3: cache_utils.py:1069 copies full_conv_states[..., -self.conv_kernel_size:] where conv_kernel_size is config.linear_conv_kernel_dim (=4, passed from modeling_qwen3_5.py:471/543). Only llama.cpp stores k-1 = 3 columns (n_embd_r uses ssm_d_conv-1, llama-hparams.cpp:204). The oldest HF column never influences any output (padding=0 update over 5 columns yields 2 outputs and only the last is kept; prefill drops the first columns via [-seq_len:]), so the two are numerically equivalent, but "new conv state = last 3 inputs" is only correct for llama.cpp. (2) causal_conv1d_fn itself slices [:, :, :seq_len] (the FIRST L_total outputs of the both-sides-padded conv, which are the causal ones); the "take the last seq_len" step is the separate mixed_qkv[:, :, -seq_len:] at modeling_qwen3_5.py:556 that drops outputs at the cached-prefix positions. Also note: when record_past is set, HF keeps the full concatenated history in conv_states (cache_utils.py:1072) rather than a 4-wide window, and the seq_len==1 fast path is skipped (:530).
- keep=True — Two qualifiers: (1) The "5 kernels" count is specific to chunk_size=64 on non-Intel GPUs (cumsum + fused kkt_solve + recompute_w_u + h + o). For chunk_size 16/32, or on Intel, the intra step falls back to unfused chunk_scaled_dot_kkt_fwd + solve_tril + recompute_w_u, i.e. 6 launches; with use_gate_in_kernel=True the cumsum is replaced by gdn_gate_chunk_cumsum (same grid (NT, B*H), fuses softplus/A_log/dt_bias). (2) BV autotune space is [32, 64] only when check_shared_mem('ada') passes; otherwise BV is fixed at 32 (chunk_delta_h.py:52). The state kernel's grid is therefore (cdiv(V,BV) * N * HV,) with N = number of sequences (B for fixed-length, len(cu_seqlens)-1 for varlen).
- keep=True — Two minor precision fixes, no change to the identity itself: (a) the DeltaNet citation should read Eq. (6)-(7) for the WY representation and Eq. (10)-(11) for the UT transform (Eq. (8)-(9) are the chunk recurrence); Sec 3.1 Eq. (3) is the pseudo-value induction. (b) In the gated case the product prod_{i<=r} alpha_i (I - beta_i k_i k_i^T) collapses to gamma_r (I - sum_i w_i k_i^T) = gamma_r I - sum_i (gamma_r w_i) k_i^T (Gated DeltaNet Sec 3.3: F_[t]^r = gamma_[t]^r P_[t]^r), not to I - sum w_i k_i^T; that gamma_r factor is exactly the gamma_r S_0 term already present in the stated d_r formula, so the recurrence as written is correct.
- keep=False — fla intra-chunk: A = strictLower(beta_i * (k_i . k_j) * exp(gcum_i - gcum_j)) (kernels use exp2 on gcum pre-scaled by 1/ln2). For chunk_size 64 on non-Intel GPUs, chunk_gated_delta_rule_fwd_kkt_solve_kernel (chunk_fwd.py:40-328) uses BC=16 sub-chunks: computes all 10 lower-triangular 16x16 blocks of K K^T in fp32 registers (K loop, BK autotuned in {32,64}; off-diagonal blocks only for sub-chunks with start < T), multiplies by exp2(g_i - g_j) with strict-lower mask on diagonal blocks and boundary masks m_tc guarding 0*inf NaN, scales rows by beta, inverts each diagonal block by row-by-row forward substitution (for i in 2..min(15, T-start): a = -A[i,:i]; a += sum_r a[r]*Ainv[r,:]; Ainv[i,:] = a; then Ainv += I), and merges with 16 tl.dot calls producing the 6 off-diagonal inverse blocks: Ainv10 = -(Ainv11@A10)@Ainv00, Ainv21, Ainv32 likewise; Ainv20 = -Ainv22@(A20@Ainv00 + A21@Ainv10), Ainv31 similarly, Ainv30 = -Ainv33@(A30@Ainv00 + A31@Ainv10 + A32@Ainv20). SOLVE_TRIL_DOT_PRECISION = tf32 if supported else ieee (fused kernel only). A is allocated in k.dtype. Unfused path (chunk_size 16/32, or Intel at any size incl. 64): chunk_scaled_dot_kkt_fwd (fp32 output) -> solve_tril, which launches ONE kernel chosen by BT: solve_tril_16x16_kernel (BT=16), merge_16x16_to_32x32_inverse_kernel (BT=32), or merge_16x16_to_64x64_inverse_kernel (BT=64); the 32/64 merge kernels do the 16x16 forward substitutions inline (no 16->32->64 chain), with DOT_PRECISION autotuned over {ieee, FLA_TRIL_PRECISION env, default ieee} when TMA is supported. recompute_w_u_fwd_kernel (wy_fast.py:43-106), grid (NT, B*HV), BK=BV=64: u = dot(A, (v*beta).to(v.dtype), allow_tf32=False); w = dot(A, (k*beta*exp2(gcum)).to(k.dtype)); fp32 accumulation; w stored in k.dtype, u in v.dtype.
- keep=True — Refinements for bit-matching the HF torch reference (the claim's substance stands): (1) beta = b.sigmoid() is computed in the model dtype (bf16 for the released checkpoints) and only cast to f32 inside torch_chunk/recurrent_gated_delta_rule; g is genuinely f32 (A_log.float(), a.float(), dt_bias promoted). (2) Qwen3NextRMSNormGated is NOT fully f32 in HF: x.float() -> x*rsqrt(mean(x^2)+eps) -> `self.weight * x.to(input_dtype)` (normalized x rounded to input dtype before the weight multiply) -> `* silu(gate.to(float32))` (promotes to f32) -> .to(input_dtype). The FLA fused kernel does everything in f32; the two differ by rounding. Norm is per value head over head_v_dim (=128), eps=config.rms_norm_eps=1e-6, plain weight (init ones) — not the (1+weight) form used by Qwen3NextRMSNorm. (3) q scale: chunk path multiplies by 128**-0.5, recurrent path divides by 128**0.5 — same math, may differ by 1 ulp; apply after l2norm in f32 to match. (4) FLA enforces chunk_size via ValueError, not assert. (5) HF pads the sequence to a multiple of chunk_size, so if tokens-per-pass != C the reference still works — C=tokens-per-pass is a design choice, not a requirement.
- keep=True — Minor precision only: Metal has two kernel implementations selected by a preprocessor branch (kernel_gated_delta_net_impl at ~2702 and a float/float2/float4 vectorized variant at ~2845); both use the decay-first order, so the claim holds for either. The CUDA kernel stores the state transposed (M[col][i] = S[i][col]) which is what makes "S^T k" a contiguous per-warp dot product.
- keep=True — Claim stands; two refinements that strengthen it: (1) WGSL's accuracy table allows exp(x)/exp2(x) an error of 3 + 2*|x| ULP, so exp of a large cumulative difference (|x| up to hundreds) can be off by ~1e-5 relative on its own, independent of the cumsum rounding — another reason to form Lambda from per-token alpha_t = exp(g_t) rather than exp(G_r - G_i). To make the chunk and sequential paths bit-identical in alpha, compute alpha_t once (e.g. in the gate pre-pass, same shader precision) and have both kernels read it, rather than each calling exp(g_t) separately. (2) The i>r masking must be a select (where/ternary), not a multiply-by-0: FLA computes exp2 for all pairs and then tl.where-masks (chunk_o.py L137-144, chunk_scaled_dot_kkt.py L74-78), which is safe only because select discards inf; a multiplicative mask would give inf*0 = NaN. With the running-product formulation this hazard disappears entirely since only i<=r products are ever formed. Minor wording nit: f32 rounding of the cumsum accumulates to roughly (r-i)*|G|*6e-8 in the difference G_r - G_i, not a single |G|*6e-8 — the 1e-6..1e-5 relative-error conclusion is unchanged.
- keep=False — Replace the Vulkan portion with: Vulkan (gated_delta_net.comp + ggml-vulkan.cpp:5844-5898): LANES_PER_COLUMN is a spec constant (shader default 32) chosen by the host per S_V at pipeline creation. If S_V >= 128 and the device supports subgroup clustered ops (the common desktop case), lanes_per_column = 8: each column is split across only 8 lanes (ROWS_PER_LANE = S_V/8 = 16 for S_v=128), COLS_PER_WG = SUBGROUP_SIZE/8 (4 columns per 32-wide subgroup, 8 per wave64), reduction via subgroupClusteredAdd(partial, 8) - so Vulkan gets an 8x thread multiplier per head, not 32x. Otherwise lanes_per_column = largest power of two dividing both S_V and subgroup_size (32 on 32-wide subgroups, 64 on AMD wave64, S_V for S_V<subgroup_size), and the reduction uses subgroupAdd (variant _nocluster) or, without subgroup arithmetic, the shared-memory butterfly reduce_add_shmem (variant _shmem; temp[SUBGROUP_SIZE], log2(LANES_PER_COLUMN) xor steps with barriers). Workgroup = one subgroup; dispatch is (H, n_seqs, S_V) elements with wg_denoms {1,1,cols_per_wg}, i.e. S_V/COLS_PER_WG workgroups per (head,seq). So the summary "CUDA/Vulkan/Metal split EACH column across a full 32-lane warp" should read "CUDA and Metal split each column across a full 32-lane warp/simdgroup (4 rows per lane at S_v=128); Vulkan uses 8 lanes per column (16 rows per lane) when clustered subgroup ops are available, else min(S_V, subgroup_size) lanes". CUDA caveat: warp_size = min(physical warp size, S_v); on AMD wave64 the column is split across 64 lanes (2 rows per lane), block (64,4), __launch_bounds__(256,2); the (32,4)/(128,2)/4-rows figures are for NVIDIA. Everything else (CUDA per-token dataflow and state-in-registers, Metal (S_v/nsg,H,n_seqs)x(32,nsg) with nsg=S_v/32 and simd_sum, WebGPU S_V=WG_SIZE=S_v grid (H,n_seqs) with private state array and sh_q/sh_k workgroup staging, CPU transposed state with ggml_vec_scale/dot/mad and 4 passes per token) is confirmed.
- keep=True — 1) Notation: gamma_r in S3 (R[r] = beta_r V - beta_r*gamma_r*ak, Oi[r] = gamma_r*aq) and gamma_{C-1} in S6 must be the CUMULATIVE decay exp(G_r) = prod_{m<=r} gate_m from chunk start, not the per-token gate; using the per-token gate there is wrong for r>0. Lambda[r][i] = exp(G_r - G_i), Lambda[r][r] = 1. 2) Shared-memory sizes: A, P, Lambda at C=16 are 1 KB each (3 KB, not <=2 KB); totals with Q in shared are ~4.2 / ~9 / ~19.2 KB for C=4/8/16 (not 6/10/18); with Q in storage at C=16 ~11.2 KB. Lambda need not be stored at all — fold it into A and P during S1 and keep only lamC[C]. 3) WebGPU limits (MDN/spec): default maxComputeWorkgroupStorageSize is 16384 bytes, so "fits 32 KB" only holds if requiredLimits raises it at requestDevice (adapter-dependent; Apple/most desktop expose >=32 KB). Likewise default maxComputeInvocationsPerWorkgroup and maxComputeWorkgroupSizeX are 256, so the 512-thread (P=4) occupancy variant and the 32 KB partial-sum buffer both need raised limits; P=2 with C=8 (256 threads, 16 KB) is the only variant inside defaults. 4) S1 bank conflicts: Ks/Qs rows have a 128-f32 pitch (multiple of 32 banks), so threads dotting different rows at the same column i hit the same bank (up to 16-way conflict at C=16). Pad the row pitch to 129 (or 132) floats, or rotate each thread's start column. S3 is unaffected (uniform address = broadcast). 5) Claim (a) is overstated: the sequential kernel's 128-long reduction chain per token can also be split into independent accumulators; the true serial dependence it cannot remove is token-to-token (vhat_t needs S_{t-1}), which the chunk form breaks into C independent reductions plus a C-step solve. 6) Minor: FLA's current tree is fwd_intra (fused kkt + solve_tril + recompute_w_u) rather than a separate recompute_w_u call, and uses log2 gates with exp2; equivalence holds since v_new = u - w@h = A^{-1}(beta v - beta e^G k.h) = A^{-1}R.
- keep=True — 1) The "M4 Max 0.8B 170 -> 213 t/s" figure is NOT in PR #19504 (searched body, issue comments, review comments, reviews). Closest real numbers there: RTX 5090 Qwen3.5-MoE Q4_K_S tg128 183.68 -> 210.80 (+15%), and Raspberry Pi 5 CPU Qwen3.5 0.8B Q4_K_M tg 10.17 -> 14.38 (+41%). Drop the M4 Max claim or re-source it. 2) The per-chunk loop has 4 mul_mats, not 5 (delta-net-base.cpp:243 v_prime, 251 v_attn, 255 attn_inter, 266 kgv); the other ops are sub/add/mul/set_inplace. 3) Nuance: when cparams.n_rs_seq > 0 (rollback snapshots), build_recurrent_attn (lines 546-572) calls ggml_gated_delta_net directly and bypasses build_delta_net, so the fused op is used regardless of fused_gdn_ar/ch and there is no chunked-graph fallback on that path. 4) CUDA reports the op unsupported under GGML_USE_MUSA (ggml-cuda.cu:5278-5284), so MUSA falls back to the chunked graph. 5) Backends implementing GGML_OP_GATED_DELTA_NET in this checkout: CPU, CUDA/HIP, Metal, Vulkan, SYCL, OpenCL, WebGPU, Hexagon, OpenVINO, ET. Chunked-kernel PRs exist but are still OPEN and not in this checkout: #26001 "CUDA: Support of GDN chunked kernel for prefill", #20377 "vulkan: chunked parallel kernel for GATED_DELTA_NET". 6) Wording: the PR body says "I plan to add the chunked implementation for CPU and CUDA"; the "TODO: Add chunked kernel for even faster pre-fill" is in the source file (gated_delta_net.cu:180), not listed in the PR text.
- keep=True — 1) Section citation: the eigenvalue remark is in arXiv 2406.06484 Sec 3.3 (DeltaNet Transformer, 'Feature map and normalization' paragraph), not Sec 3.1; the sentence 'The inverse of lower triangular matrices could be solved efficiently using forward substitution' is in Sec 3.2 (after Eq. 10). The same row loop also appears in the paper's appendix PyTorch listing. 2) HF nuance: in transformers' torch_chunk_gated_delta_rule the row-recursive inverse is used only under is_torchdynamo_exporting(); the default path is torch.linalg.solve_triangular(ut_system, v_beta/decayed_k_beta, upper=False, unitriangular=True), i.e. HF already performs the direct (I+A)D=R solve without forming T -- cite this as further support. HF's A includes the pairwise gate decay (Lambda), and its loop is written as row + (row.unsqueeze(-1)*ut[:i,:i]).sum(-2), mathematically identical. 3) solve_tril.py nuance: the 16x16 kernel uses elementwise multiply + tl.sum in fp32 (no tl.dot); DOT_PRECISION applies to the 32/64 merge kernels and defaults to 'ieee' but FLA_TRIL_PRECISION env can enable tf32/tf32x3 on TMA hardware. The merges are the block-lower-triangular inverse identity Ai_21 = -Ai_22 A_21 Ai_11 (calling it 'block-Schur' is loose). 4) Wording: the product of I - beta k k^T is nonexpansive (spectral norm exactly 1 since eigenvalue 1 has multiplicity d-1), not strictly a contraction. Stronger statement available: |T[r,i]| = beta_r |k_r^T (prod_{i<j<r}(I - beta_j k_j k_j^T)) k_i| <= beta_r < 1, so entries of T (and of D relative to R) are bounded; numerically fp32 forward substitution at C=16 stays within ~4e-7 relative of fp64 across adversarial key configurations. 5) 'multiplicity 127' assumes d_k = 128 (paper says d-1). 6) The '128-length dependent reductions per token' figure for the sequential kernel is project-specific and not verifiable from the cited sources.
- keep=True — Optional precision tweaks only: (1) results were not written to micro.txt; cite dnbench.js stdout (re-run reproduces: V0 9.18 / V1 9.61 / V1u 8.06 / V2 7.41 / V3 4.93 (1.86x) / V4 5.84 (1.57x) ms). (2) "288 MB" should read 288 MiB (~302 MB); V0 traffic is ~2.3 GiB. (3) The in-engine skip-delta range "1.3-7.7 ms" is not fully supported by saved files: breakdown.txt gives ~1.3 ms, breakdown8.txt gives ~14.3 ms (noisy batched run) and ~1.5 ms (single); phrase it as "skip-delta measurements are noisy (1.3-14 ms across runs)" rather than a firm 1.3-7.7 range.
- keep=True — Replace the last two sentences ("Older versions clamped ... all exponents <=0") with: "Transformers has never clamped these exponents: initial commit b9282355be, releases v4.57.0-v5.2.0, and current main all rely on g<=0 (docstring: 'entries must be <= 0') so cum_last - cum_t <= 0 and exp <= 1. The `torch.clamp(g_cum[:, :, -1:] - g_cum, max=50.0)` snippet is a comment in llama.cpp's initial Qwen3-Next commit (ff55414c42, src/models/qwen3next.cpp:316-317) quoting that author's own torch reference, not HF code; the comment is gone from current llama.cpp master." Also note: in qwen3_next the identical functions are at modeling_qwen3_next.py:377-511 (not 257-390), and in v4.57.0 l2norm ran in the input dtype before the fp32 cast, whereas current code casts first.
- keep=False — Numerical caveat 2: expected deviation vs the sequential recurrence and how to validate it.
The chunk form reorders sums: o_r accumulates sum_{i<=r} Lambda (k_i.q_r) d_i at once instead of progressively through S, and S_C accumulates rank-C at once. In f32 with L2-normalised q/k (|k_r.k_i|<=1, |beta|<1), C=64, d=128, expect max|diff|/max|ref| and relative-L2 of ~5e-7 on outputs and on the final state (re-derived in numpy: 6.7e-7 outputs, 5.6e-7 state), i.e. ~2-3x looser than the ~1e-7 (2-4e-7 rel-L2) you see for batched-vs-sequential today; budget ~1e-6 as the ceiling. That is still >1000x below bf16 weight noise (bf16 eps = 3.9e-3) and further below int4/int8 quantisation noise. Validate with a normalised metric — max|diff|/max|ref| or relative L2 — NOT elementwise max relative difference: near-zero entries push the elementwise figure to 1e-3..1e-2 for a correct kernel. The error grows with chunk size C (more terms per reordered sum and a larger triangular solve), so re-measure if you change C. Cancellation in d_r = beta(v - predicted) has the same magnitude in both forms, but the chunk form performs it as u_r - w_r.S_prev with the intra-chunk part folded into the (I+tril)^-1 solve, so the rounding path differs.
Precision of intermediates: the reference PyTorch paths (fla naive_chunk_gated_delta_rule, HF torch_chunk_gated_delta_rule) cast q,k,v,beta,g to float32 and keep A, the solve, pseudo-values and the state entirely in fp32; the state is returned in fp32 and only the output is cast back. FLA's production Triton kernel is looser: chunk_gated_delta_rule_fwd_intra calls solve_tril with output_dtype=k.dtype (the torch.float default is overridden; the fused BT=64 path allocates A in k.dtype directly), recompute_w_u_fwd stores w/u in k/v dtype, and chunk_gated_delta_rule_fwd_h stores the per-chunk state checkpoints h in k.dtype — all with fp32 accumulation inside the matmuls; only the in-register running state and final_state (new_zeros dtype=torch.float32) are fp32. Practical rule for a WebGPU port: keep the running state S and the final state in f32 unconditionally (both references and FLA agree); keep A, P/w, D/u in f32 for the first correct implementation and only consider f16 storage of those (never S) after validating against the f32 kernel, noting f16 has less range than the bf16 FLA uses.
Padding is exact (re-derived and confirmed bitwise): a padded token with q=k=v=0, beta=0, g=0 gives an identity row in A, u=w=0, d=0, alpha=exp(0)=1 -> S unchanged, o=0, and cum-decay stays flat so real tokens' inter-chunk decay factors are unchanged; every step is an exact IEEE op. So zero-pad short passes to C and discard pad outputs (fla naive uses F.pad with zeros on q/k/v/beta/g; HF uses pad_size=(chunk_size - T % chunk_size) % chunk_size with the same zero padding).
- keep=True — (1) Dispatch line is wrong: the dn_delta_mc dispatch is at engine/qwen35.js:1164 (`this._dMC(p, "dn_delta_mc", M.delta, D.nVH * 128, 128, 1)`), not :1097. Line 1097 is the `} else {` that opens the DeltaNet bind-group branch; the delta_mc bind group itself is built at qwen35.js:1065-1066. (2) Occupancy figure: GB10 is CC 12.1 with max 1536 threads/SM = 48 warps/SM (queried via cuDeviceGetAttribute), so 4 warps/SM is ~8% occupancy (4/48), not ~6% (which assumes a 64-warp/SM part). Optional precision: GB10 L2 is 24 MB (25165824 B), which is why the 3 MB per-layer state is L2-resident. The aliasing/serialization explanation is unverified (no SASS inspected); the number-of-bytes-moved and latency-bound conclusions stand independently of it.
- keep=True — Refinements (the claim's conclusion stands): (1) Per-thread work is ~4x128 FMA-class ops per column (3 FMA + 1 mul in the decay/dot loop, 1 FMA in the rank-1 update), not 3x128; order of magnitude unchanged. (2) The loads in the loop are not address-dependent (indices are computed); the serial chain is through the 3 accumulators plus a store->load ordering hazard on the same read_write buffer dlm_s, which is why S traffic (4 accesses/element/column) rather than a pointer chase is the cost. (3) Hypothesis (a) is the current kernel's actual state and is already measured at ~7-8.5 ms per 4-col pass (~2 ms/token, kernel-plan-3.md:1585), so the rewrite's ceiling is ~1.5 ms/token, ~4% of the 37 ms marginal. (4) No 8-column breakdown exists: bench_breakdown_deno.js hardcodes 4 ids and the default batchCols=4 (qwen35.js:531), so the 37 ms/token has no per-kernel attribution; before touching dn_delta_mc, run the breakdown with batchCols: 8 and 8 ids and also compare the 4-col figure at pos ~86 vs the synthetic pos<=28 run (the 148 ms end-to-end pass vs 101.5 ms prefill-only breakdown gap suggests attention growth, per-pass readback/JS overhead, or head work is part of the marginal). Likely (b) contributors from existing data: batched f32 matvec ALU scaling (8 cols cost 1.6-1.8x single in the DP4a microbench, kernel-plan-3.md:1628, vs ~1.16x at 4 cols), per-column head/readback (~20-37 ms per 4-col pass), and every _mc family looping columns serially. (5) Fix the wording in docs/bench-log.md (Sep 1 BCOLS=8 row: "8-col pass ≈ 4-col pass" means equal throughput, i.e. pass time doubled, and "bound by the serial DeltaNet recurrence" is unsupported) and kernel-plan-3.md:1585 ("dominated by dn_delta_mc" -- it is ~7% of the prefill pass; matvecs are ~94%).
- keep=True — 1) Register-file sentence is wrong: 128 threads x 128 f32 = 16K 32-bit registers = 64 KB, which is one quarter of an NVIDIA SM's 256 KB (64K-register) file, not 'exactly one SM's register share'. The relevant pressure is per-thread: 128 f32 for col_s alone against the 255-register/thread cap (plus loop temporaries), which is why V1u is tight and V3 (32 f32/thread) is comfortable. With 48 heads = 48 WGs on GB10's 48 SMs, occupancy is one WG per SM either way, so the register budget is not a limiter. 2) Unroll caveat is overstated: V1 dyn-loop (128-element private array) did lose the register benefit (9.58 vs 8.08 ms), but the bench's V2d (256 thr, dynamic loop over 64 rows) ran 7.360 ms vs 7.389 ms for unrolled V2, i.e. identical; the compiler already unrolls the constant-trip 64-row loop. So generating unrolled row loops is cheap insurance for the tiled kernel, not a demonstrated necessity at R <= 64. 3) Minor additions: the '16-32 KB limit' should read 'the 16 KB WebGPU default (GB10 exposes 48 KB; either way 64 KB does not fit)'; and V3 needs a non-default maxComputeInvocationsPerWorkgroup >= 512 requested at requestDevice (default is 256), which dnbench.js does at lines 13-16 and guards at line 228 - workgroup memory is Chrome/Safari safe, but the 512-thread WG must be gated on that limit.
- keep=True — 1) Step 1 needs TWO barriers per column, not one: llama.cpp's wgsl has workgroupBarrier() at line 92 (after staging) AND line 141 (end of loop) — the second prevents a fast thread from overwriting sh_q/sh_k for column t+1 while slow threads still read column t (or double-buffer the staging arrays to get back to one). 2) dn_delta_mc's early `if (h >= nVH || j >= dS) { return; }` (qwen35.js:297) precedes any barrier; `j >= dS` depends on local_invocation_id, so WGSL uniformity analysis will reject a workgroupBarrier() after it — replace with a guarded body (or drop the check: dispatch is already exactly nVH x 128). 3) Step 2's 'workgroup 512/1024' variant needs requiredLimits maxComputeInvocationsPerWorkgroup / maxComputeWorkgroupSizeX raised above the default 256; SwarmLLM's requestDevice only raises buffer limits, so the '4-8 workgroups per head, each 128 threads = 16-32 columns x R lanes' variant (the Vulkan gl_WorkGroupID.z scheme) is the one that works by default. 4) Vulkan's reduce_add_shmem costs 2*log2(R)+2 barriers per reduction (temp write + barrier, then per step read/barrier/add/barrier, then a trailing barrier), not log2(R). SwarmLLM's formulation has three partials (vhat, sq, kq); either pack sq and kq into one vec2 reduction, or adopt llama.cpp's order (attn = sum(state_new*q) after the update) which needs exactly two reductions per token; that changes op order (bit-close, as stated). Subgroup ops are not an option per engine.js:410 (no subgroups in Safari 26). 5) Step 3(a): the A in (I+A)^-1 is strictly lower (tril(-1)) — HF uses unitriangular=True, so the diagonal beta_i|k_i|^2 is excluded. The chunk path needs log-space g (per-token softplus(alpha+dt)*A) for the cumsum; SwarmLLM's dn_pre/dn_gates emit only exp(...) (dlm_decay), so keep the pre-exp value. 6) Workgroup-memory budgets: the default maxComputeWorkgroupStorageSize is 16384 bytes, so a 64x64 f32 (I+A)^-1 (16KB) or a 128x32 f32 state tile (16KB) in var<workgroup> uses the entire default budget with nothing left for staging — either request a larger limit or hold the state tile in per-thread registers (128 threads x 32 f32, like Vulkan's s_shard / fla's b_h) and only stage the 64x128 u/w/k rows. 7) Final S from the chunked path equals the recurrent one mathematically (zero-padded tokens have k=v=beta=g=0 and leave S unchanged) but not bit-identically — different summation order — so validate against ref_q38.mjs with a tolerance, not equality.
- keep=False — vLLM prefill for Qwen3-Next/Qwen3.5 GDN (both models instantiate QwenGatedDeltaNetAttention) uses chunk size 64 on all three GPU backends (vendored fla Triton, FLA_CHUNK_SIZE=64 at third_party/flash_linear_attention/ops/utils.py:31; FlashInfer gdn_prefill, chunk size 64 per upstream; CuTeDSL Blackwell kernels asserting BT==64 and K==V==128) and on CPU (csrc/cpu/sgl-kernels/fla.cpp:2294 `constexpr int CHUNK_SIZE = 64`, gathers/mutates the state pool in place via initial_state_indices). Prefill pipeline as described (fused_post_conv_prep -> ChunkGatedDeltaRule dispatch; vendored chunk: cumsum -> kkt (fp32) -> solve_tril (k.dtype) -> recompute_w_u -> fwd_h (grid (V/BV, N*H), BV autotuned in [32,64], [BV,64] fp32 accumulators, (V,K) layout) -> chunk_fwd_o writing into core_attn_out; bf16 assertion; FlashInfer path casts state/g/beta to fp32 and passes exp(g); CuTeDSL: kkt_inv_uw 12 warps, h 10 warps with h_dtype fp32 or bf16 chosen from the state dtype, o 10 warps). GPU prefill state handling: initial_state = ssm_state[prefill_state_indices] (gather copy), rows without prior state zeroed, final state scattered back with ssm_state[idx] = final.to(ssm_state.dtype) (qwen_gdn_linear_attn.py:1517-1536) - not in place. Decode: spec and non-spec decode call fused_sigmoid_gating_delta_rule_update (ops/fused_sigmoid_gating.py) - a Triton kernel fusing the sigmoid/softplus gating with the recurrence, BV=min(next_pow2(V),32), num_warps=4, grid (NK,NV,N*HV), reading/writing the paged state in place (INPLACE_FINAL_STATE) via ssm_state_indices; the packed non-spec decode-only path uses fused_recurrent_gated_delta_rule_packed_decode (fused_recurrent.py:343, num_warps=1, BV=32, grid (NV,B*HV) or (NV,HV,B)). fla's plain fused_recurrent_gated_delta_rule_fwd is not used by this layer. State cache dtype: configurable via mamba_cache_dtype/mamba_ssm_cache_dtype in {auto, float32, float16, bfloat16} (vllm/config/cache.py:70); the fused-norm packed fast path requires state dtype in (fp32, bf16) (FUSED_GDN_STATE_DTYPES), the CuTeDSL h kernel supports fp32/bf16 states, and the CPU op accepts fp16/bf16/fp32.
- keep=False — Option (b) 'all heads x tiles as more workgroups': splitting a head by COLUMNS needs no reduction but keeps the same 6144 threads (48 heads x 128 column-owners), only changing WG shape - no gain. Splitting by ROWS adds threads but vhat_j = sum_i S[i,j] k_i needs every row, and WebGPU has no intra-dispatch cross-workgroup barrier, so each token costs 2 dispatches (A: per-tile partial vhat/sq to a global partials buffer; B: reduce, d, S tile update with decay folded in, o). With the engine's NC<=4 that is 2*nCols*48 = 96-384 dispatches per DN pass vs 48 today (48-336 extra), and S is streamed through L2 3x64 KB per head-token (vs 4x64 KB in the current dn_delta_mc, or 2x64 KB per pass for register-resident tiles). MEASURED (dnbench.js, GB10, 48 layers, valid: relDiff S~5e-8, o~2.3e-7): NC=4: V0 current 9.10 ms, V3 512-thr in-WG 4x128 tiles 4.95 ms, V5(b) 4/8 tiles 8.36/8.27 ms; NC=1: V0 5.61, V3 3.53, V5 3.97/3.93 ms. So (b) does beat the current kernel in isolation (~8-30%) but loses to in-WG row tiling (V3: 1.7x faster at NC=4, 1.12x at NC=1) which gets the same parallelism in ONE dispatch per layer with S in registers - and in the real engine each extra dispatch also adds to the per-token dispatch chain. Not recommended; V3-style in-WG tiling (512 threads, 4x128) is the option to take. Note: an earlier note that V5 had 'no valid measurement' due to a bind-group/auto-layout mismatch is obsolete - the current file's stageA references all bindings and runs correctly.
- keep=True — Two small number/wording fixes, conclusion unchanged: (a) The register-variant S traffic is 288 MiB = 302 MB/pass; at the 273 GB/s DRAM peak that is 1.1 ms and halving it saves ~0.55 ms, not "<0.5 ms"; at the 184 GB/s the bench-log measured as achievable via WebGPU it is 1.6 ms / ~0.8 ms saved. Both are upper bounds: the measured best register variant (V3, 5.0 ms/pass) moves S at only ~60 GB/s effective, so the kernel is latency/serial-recurrence bound and the real f16 gain would be well under that -- at most ~10-15% of a 5 ms pass, versus the 45% V0->V3 gain that costs nothing numerically. (b) "compounds through decay/update" should read "accumulates": the update S <- dec*S + k d^T with d = beta(v - S^T k) is contractive (decay <= 1, beta in (0,1), unit k), so per-token rounding error is bounded (~u/(1-dec), i.e. up to ~10-100x u for dec 0.9-0.99) rather than growing exponentially; it still never returns to f32 precision, so the >1e-7 and greedy-flip conclusions stand. Optional addition: besides the extra code path, Deno/wgpu has open issue #23125 (f16 compute producing zeros), so an f16 S path would also need the load-time self-test the plan already prescribes for f16 x.
- keep=False — Precision across implementations: the running state is accumulated in fp32 registers everywhere (fla b_h tl.float32, HF fp32 tensors, llama.cpp float, vLLM CuTeDSL fp32 accumulators), but the EMITTED final state is fp32 only in fla (chunk_delta_h.py: final_state = k.new_zeros(..., dtype=torch.float32)), HF (uncast fp32 last_recurrent_state; only core_attn_out is cast back to initial_dtype), and llama.cpp (F32 asserted on q,k,v,g,beta,state at ggml.c:6294-6299). In vLLM CuTeDSL the final state is torch.empty_like(initial_state) (gdn_chunk_cutedsl/__init__.py:211) with h_dtype taken from h0.dtype (kernel_h.py:734-736); the SSM cache dtype follows mamba_ssm_cache_dtype, which for "auto" inherits the model/conv dtype (mamba_utils.py:105-106), i.e. bf16 by default; FUSED_GDN_STATE_DTYPES=(float32,bfloat16). Per-chunk h snapshots are bf16 in vLLM (kernel_h.py:686) and k.dtype in fla (chunk_h.py:338, states_in_fp32=False); fla's A, w, u are also stored in k.dtype (chunk_fwd.py:384, wy_fast.py:273-274). Gate cumsums are fp32 in all four (fla chunk_local_cumsum output_dtype=torch.float with scale=RCP_LN2=1.4426950216 so kernels use exp2; gdn_gate_chunk_cumsum fp32; HF cum_decay on fp32; llama.cpp ggml_cumsum on F32 g_cs; vLLM g_cu fp32). Chunked decay factors are always exp/exp2 of differences of cumulative log-gates (fla exp2(b_g_last - b_g), exp2(b_g_i - b_g_j) masked lower; HF exp(cum_i - cum_j) with upper masked to -inf; llama.cpp ggml_exp(ggml_tri(ggml_sub(g_cs_j, g_cs_i))) and exp(g_last); vLLM exp(g_cu[last]) and exp(g_cu[last]-g_cu[t])), so exponents are <= 0 given g <= 0; the sequential llama.cpp op instead applies expf(g_t) per token. l2norm: fla computes 1/sqrt(sum x^2 + eps) in fp32 with eps=1e-6 (l2norm.py:105); HF's rsqrt(sum + eps) has the same eps placement; the user's dn_l2_mc uses 1/max(sqrt(ss), eps2), which is a different eps semantics from both. Triangular solve precision: fla's standalone solve_tril stores with fp_downcast_rounding='rtne' and its dot precision defaults to 'ieee' via FLA_TRIL_PRECISION (solve_tril.py:19,89,175) - but that is only the unfused path (chunk_size != 64 or Intel). The default fused kkt+solve_tril kernel used for chunk_size=64 (chunk_fwd.py) uses SOLVE_TRIL_DOT_PRECISION='tf32' when IS_TF32_SUPPORTED, else 'ieee', with no explicit rtne on its stores. In wy_fast.py only the u = A @ (v*beta) dot has allow_tf32=False (line 89); the w dot (line 105) does not. HF uses fp32 torch.linalg.solve_triangular (or fp32 forward substitution when exporting); llama.cpp uses ggml_solve_tri on F32. None of the fp32 implementations claims bitwise equality between chunked and sequential forms; fla's tolerance tests (tests/ops/test_gdn.py, assert_close at 0.002-0.008) compare against naive_recurrent_gated_delta_rule (naive.py:13-64), not naive_chunk_gated_delta_rule (which starts at naive.py:67 and is not used by any test).
- keep=False — Per-column-serial / latency-bound ops in the batched pass (scratchpad/micro.js isolated timing, 8 columns, scratchpad/micro.txt, measured at commit e07c0e0): head_norm_mc 3.4 ms (16 full layers, constant in nCols: 3.34/3.38/3.38 at 1/4/8), dn_l2_mc 2.9 ms, dn_gates_mc 1.5 ms (48 DN layers, one 1-WG dispatch of nVH=48 threads per column, columns in y), dn_conv_mc 1.7 ms, rmsnorm_mc+add_res_mc 4.5 ms for the synthetic 4-dispatch/layer family (256 dispatches; ~17 us per dispatch). Dispatch overhead on this Dawn/Vulkan stack ~10-20 us; the "128 rmsnorm dispatches ~2.2 ms" figure is inferred (half of 4.5), not measured.

CURRENT working tree (qwen35.js, uncommitted after 794236d) differs from the measured tree: dn_gates_mc and dn_l2_mc are no longer dispatched in the batched pass — they are fused into dn_pre_mc (one 128-thread WG per column, line 1163, kernel 402-420); the residual add is folded into the *_coop_b_acc matvec variants so add_res_mc is skipped for o/out/down (lines 1155/1167/1179), leaving 128 rmsnorm_mc dispatches per pass, not 256. Current line refs: dn_conv_mc/dn_l2_mc 242-284 (unchanged); head_norm_mc 465-477 (was 408 at e07c0e0); _encodeLayerBatch 1127-1182, K/V copies 1141-1144 (2*nCols*16 = 256 copies at 8 cols, sitting between compute passes), per-column silu_mul 1176 (only when _guOp returns null: coop off or gate/up quant kinds differ); prefill per-column mtpRun 1370; <NC tail via prefillToken 1375-1379.

Dispatch count per batched pass: at e07c0e0 it was 48x16 + 16x(16+3n) = 1408 at n=8 (the 48x12 + 16x(9+3n) ~1100 formula drops the FFN block [norm2, gu, down, add] and sigmoid/o/add); in the current tree 48x13 + 16x(14+3n) = 1232 at n=8, plus 64*n silu dispatches when gu is absent. Either way ~12-25 ms of pure launch cost.

head_norm_mc: workgroup_size(32), each thread runs two serial headDim=256 loops over global memory -> ~0.21 ms/layer regardless of columns (same shape problem as dn_l2_mc, 32 threads x two serial dState=128 loops). dn_conv_mc: in-kernel serial column loop with 4 FMAs on register state; column-parallel form y[col] = w3 x[col] + w2 x[col-1] + w1 x[col-2] + w0 x[col-3] with x[-k] from the 3-entry carried state is correct but gains nothing (1.5-1.7 ms total).

Skip-family method (bench_breakdown_deno.js, eng.skip Set) is too noisy for families under ~10 ms: saved breakdown.txt (4 cols) gives -5.6 (dn_conv), -2.0 (gates/l2/gatenorm), -3.0 (attention), -4.2 ms (qsplit/head_norm/rope/sigmoid); breakdown8.txt gives +0.6 ms for that family. No saved output contains a -49 ms value; drop that specific figure. Use micro.js isolated timing for these families.
- keep=False — Cheapest path to a real ~1.4x (8 cols) to ~2.5x (16 cols) prefill gain: fix the batched GEMV's x-reuse (rows per workgroup), not the recurrence; a V3 dn_delta_mc rewrite is a cheap ~7 ms/pass side win at 8 cols (~12 ms at 16). Budget at 8 columns (breakdown8.txt / micro.txt, GB10): 211-223 ms = ~170 ms matvec_b + ~14 ms dn_delta_mc + ~26-38 ms other kernels/copies/dispatch. Single-token matvec is ~83 ms (~14-15 GB streamed, ~175 GB/s), so matvec_b at 8 cols is 2.05x the single-token cost; bringing it to ~1.3x (~108 ms) drops the 8-col pass to ~150-160 ms = 50-54 tok/s (~1.4x); at 16 columns with the same kernel efficiency ~165-185 ms = 86-97 tok/s (~2.3-2.6x). The non-matvec cost is NOT fixed: micro.txt shows non-matvec at nCols=1/4/8 = 19.4/27.2/40.2 ms, i.e. ~16 ms fixed + ~3 ms/column (dn_delta ~1.1 ms/col; per-column attn_scores/softmax/out dispatches, 3 per column per full-attn layer; copies 2 per column), so it grows to ~60 ms at 16 cols and must be held down alongside the GEMV fix. Dispatch count is ~12 per DeltaNet layer and ~36 + 2*nCols per full-attn layer (~18-20 avg over 64 layers). GEMV diagnosis from coopWGSL: each lane loads 2*COLS vec4 of x (64 f32 registers at COLS=8, 128 at 16) per 32-elem block versus ROWSB u32 of Q4 weight, and every one of the dOut/ROWSB workgroups re-reads all of x, so x traffic is ~COLS*4/(ROWSB*0.5625) ~ 14x the weight bytes at COLS=8/ROWSB=4 (~700 MB per gate/up or down matvec, L2-served). rows.txt confirms the lever is rows per workgroup, not fewer accumulators: BCOLS=8 ROWSB=4 30.9 tok/s > ROWSB=2 29.0 > ROWSB=8 27.0 (spills). So: do NOT reduce ROWSB; and staging x through workgroup memory in the current layout saves nothing (lanes (bl, qt) read disjoint x slices). The direction that actually amortizes x is a 2D tile: threads split over rows as well as dIn (e.g. 64-128 rows x COLS per workgroup), x block staged once per 32-elem block in workgroup memory and reused across all rows in the tile, weights still decoded once per COLS columns; prefill can use a different (COLS, ROWSB/tile) instantiation than verify. Recurrence: dnbench V3 (512 threads, 4x128 register tiles) is 14.03 -> 6.92 ms per 48-layer x 8-token pass (relDiff S=7e-8, o=2.3e-7), keeps bind groups/dispatch count.
- keep=True — Minor fixes: (1) qwen35.js line numbers are stale (the file is under concurrent edit; _dCol moved 972->981 during verification): per-column attention bind groups are at ~1092-1094 (inside the cols block ~1086-1096), _encodeLayerBatch starts at ~1127, the KV copies are at ~1141-1144 and the serial column loop at ~1147-1152 — cite by symbol, not line. engine.js 321-376 is correct. (2) The 'ideal ~2 ms K/V read' assumes every column re-reads the layer's K/V at the 273 GB/s DRAM peak (~529 MB at basePos 500); at the 184 GB/s achievable streaming read measured on GB10 (docs/bench-log.md) it is ~2.9 ms, and if the 4 MB/layer K/V stays cache-resident across the 8 columns the DRAM floor is ~0.4 ms — so the third-step estimate of 3-5 ms is 'at the per-column-reread floor', not the absolute floor. (3) TVM's WebGPU fallback (tile_z=8, num_warps=2) triggers when ceil(d/128)*ceil(bits/16) >= 4, i.e. d >= 385 for f16 (so d=512), but for f32 it already triggers at d >= 129: TVM's own config for the f32/d=256 case here would be tile_x=8, tile_z=8, num_warps=2 (64 threads), not 16/16/128. Also, bdy=group_size in the decode kernel is halved while bdx*bdy exceeds the thread limit. (4) Measurements were taken with Deno/wgpu (Vulkan) on GB10, not in a browser.
